# Notes

> Tacit knowledge an agent can't infer from reading code.

## Build & Test

- **Optional-mod CI policy:** Mekanism、Iron Furnaces、Farmer's Delight compatibility各使用一個代表性present-mod fixture，加上正常absent-mod base run。不要增加多版本matrix，也不要把fixture artifact變成玩家端exact pin；其他已安裝版本的不相容由使用者回報後修正。
- **GameTest tasks must be separate and sequential Gradle invocations:** `runGameTestServer`、`runRecipeAddonGameTestServer`、`runMekanismGameTestServer`、`runIronFurnacesGameTestServer`、`runFarmersDelightGameTestServer`共用`run/logs/latest.log`、`run/world`與run directory；不可把多個task放進同一條`./gradlew ...`，也不可從不同terminal/process並行執行。並行曾在world save期間觸發`NoSuchFileException`並污染下一個fixture。每次先確認前一個Gradle process已退出，再精確清除`run/world`並執行下一條；CI與本機完整gate都依下表逐條執行。
- **本機 GameTest 前清除舊 world：** 五個 task 共用 ignored `run/world`。曾有 2.4 GB 舊 Core repository 讓下一個 fixture 在 server start 重播大量 NBT；本機每次 GameTest invocation 前只精確刪除 `run/world`，不可刪整個 `run/`。CI 的 clean checkout 不受此問題影響。
- **GameTest 不可跨 delay 持有全域 gamerule：** 同一batch的GameTests會並行推進。測`doTileDrops=false`時只能在實際行為的同步callback內保存、切換、`finally`還原；不可在`runAfterDelay(...)`前關閉後等數tick，否則其他Wrench/drop測試會隨排程隨機失去掉落，形成假紅。

先設 `JAVA_HOME`(JDK 21,不在 PATH):

```
export JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home
```

| 用途 | 指令 |
|------|------|
| 編譯(最快檢查) | `./gradlew compileJava` |
| 公開 Recipe Family API 外部 package 編譯 | `./gradlew compileApiTestJava` |
| 完整建置 | `./gradlew build` |
| 版本化建置 + Prism dev 部署 | `python3 scripts/deploy_prism_dev.py`（自動 bump `mod_version` patch、`./gradlew build`、下載並驗證釘選 Fusion 1.2.12 SHA-512，將 Magic Storage + Fusion staging/backup/replace 視為同一 rollback transaction；任一步失敗都還原原本版本與兩族 active jars） |
| 自動測試(SelfTest 於 mod init + GameTest) | `./gradlew runGameTestServer`（post-run gate固定要求372個GameTests全過，SelfTest固定224741，且任何`SelfTest: ... TESTS FAILED!`都讓Gradle失敗） |
| Recipe/addon API 整合測試 | `./gradlew runRecipeAddonGameTestServer`（獨立載入`magic_storage_recipe_fixture`，固定要求17個custom recipe、polymorphic station、held container、generic/native Bus、typed escrow與resource-kind atomic API GameTests全過；同樣拒絕任何SelfTest failure） |
| Mekanism 整合測試 | `./gradlew runMekanismGameTestServer`（隔離載入一個代表版本，固定要求17個chemical capability + Crushing/Enriching/Smelting/Combining/Pressurized Reaction GameTests全過；不是玩家端exact dependency pin） |
| Iron Furnaces 整合測試 | `./gradlew runIronFurnacesGameTestServer`（隔離載入一個代表版本，固定要求3個variant discovery、live config rate與fractional generation GameTests全過） |
| Farmer's Delight 整合測試 | `./gradlew runFarmersDelightGameTestServer`（隔離載入一個代表版本，固定要求5個Cooking Pot ingredients/container/remainder/work/Fuel/rollback GameTests全過） |
| CI(GitHub Actions) | `.github/workflows/ci.yml`:JDK 21 + build + EMI minimum/latest-compatible Modrinth release compile + 五個依序隔離的 GameTest runs + Python unittest + runData drift check，並上傳jar/log/report artifacts |
| Optional client smoke | `.github/workflows/client-smoke.yml`只可手動dispatch；先放入required Patchouli、釘選Fusion與最新`[1.1.24,2)`對應的exact NeoForge 1.21.1 EMI，再用HeadlessMC / `mc-runtime-test`啟client；step硬限10分鐘，且**不**代表GUI layout目視通過 |
| CD(GitHub Release) | push tag`v<mod_version>`觸發`.github/workflows/release.yml`；workflow先驗tag等於`gradle.properties`的`mod_version`，再重跑包含EMI minimum/latest compile在內的全部CI gates、產生release notes並發布jar |
| **GUI session / 需要目視時** | `python3 scripts/run_prism_gui_session.py --scenario <scenario>`；終端通用版面與跨終端/重啟偏好持久化用 `terminal-left-rail`，Bus 設定/方向模型用 `bus-configuration`，Fuel 頁用 `crafting-fuel-page`（要求normal Prism已開啟並完成account initialization，之後清除 instance 的 Computer Use wrapper、重建固定 world、以官方 CLI `prismlauncher -l dev -w MagicStorageGuiTest -o MagicStorageBot`啟動既有dev instance、掃本次 launcher/Minecraft log + checklist；若Prism未warm會在改world前fail） |
| Prism dev + 使用者手動 GUI 驗證 | visual scenario 到 READY 後由使用者接手並完成 fullscreen gate/checklist;見下方「Prism dev / manual handoff」 |

`scripts/deploy_prism_dev.py` 目前沒有 CLI argument parser；**不要用 `--help` 探測**，任何直接執行都會真的 bump patch、build 並部署。它只適合「建立下一個 patch 版本」；若 `mod_version` 已 bump/commit，只想重新部署 exact current jar，不可再執行，否則會多 bump 一版。exact-current deploy 尚無正式 CLI，runner 的 deploy-required 訊息也尚未區分兩種情境，已列入 roadmap。要閱讀用法直接看本表/腳本，測試 helper 則只 import module，不可呼叫 `main()`。

GitHub public repo:https://github.com/swear01/Magic_Storage 。目前版本以 `gradle.properties` 的唯一 `mod_version` 為準；release tag 必須是同值的 `v<mod_version>`，不可在文件硬編某個 patch。CI/CD 會保存 `build/ci-logs/**`、`run/logs/**`、`run/crash-reports/**`、`build/reports/**` 方便遠端失敗排查。`client-smoke.yml` 是 workflow_dispatch-only 的 client boot/resource smoke；必須把metadata required的Patchouli、資源測試需要的Fusion與exact EMI一起放入，不能只啟Magic Storage + EMI後把missing dependency畫面誤當smoke。HeadlessMC未自行退出時由10分鐘step timeout明確失敗。GUI/Patchouli/視覺變更仍必須跑下方 Prism dev / manual handoff session runner;CI/GameTest/client-smoke 都不能替代目視 GUI 驗證。

Prism runner 的 Python unittest 必須與 host 安裝狀態無關。測 `verify_prism_version` 時要在 temporary directory 建立假的 `.app/Contents/MacOS/prismlauncher` 並 mock subprocess output；不可直接依賴 macOS 的 `/Applications/Prism Launcher.app`，否則本機會綠、Linux GitHub runner會在版本斷言前因 executable 不存在而失敗。

2026-07-22 Terminal preference gotcha：以 RS2 的 client-global grid config 為主，不採AE2的per-player/per-terminal方向。終端偏好不是per-player、per-world或per-block NBT，而是physical client的單一NeoForge CLIENT config（`config/magic_storage-client.toml`），同一Minecraft profile開啟任何Storage/Crafting/Remote Terminal都共用。持久化共用sort mode/order、search mode、resource view，以及Crafting專用page、use-player-inventory、output destination、Fuel Target；不保存search query、scroll offset、selected item/recipe或adaptive visible rows。畫面開啟時client只送偏好值，server menu仍驗證、套用並透過既有data slots回傳；`TerminalPreferenceSession`在完整server acknowledgement前以磁碟偏好作**純畫面 presentation overlay**，避免client menu逐格收到data slots前先顯示預設頁/按鈕再跳動，ack後立即只呈現server menu值；它同時等ack後才保存後續變更，禁止用開啟瞬間的menu defaults覆寫config。overlay不可寫menu、Core、resource或network state，server仍是唯一權威。`TerminalSettingsPacket`新增偏好wire欄位後，NeoForge payload protocol必須維持`1.1`；不可退回`1.0`讓舊client/server誤解封包。`defineInList`的候選清單必須允許NeoForge首次建立空config時用`null`做validation；不可換回拒絕`contains(null)`的JDK immutable list，SelfTest以`SPEC.correct(CommentedConfig.inMemory())`守住第一次啟動不崩潰且`fuelTarget=auto`。採用模式核對RS2 [`ConfigImpl`](https://github.com/refinedmods/refinedstorage2/blob/0643e6f4bc25f12dc01893916c53d393ecdc08d0/refinedstorage-neoforge/src/main/java/com/refinedmods/refinedstorage/neoforge/ConfigImpl.java#L460-L615) 與 [`AbstractGridContainerMenu`](https://github.com/refinedmods/refinedstorage2/blob/0643e6f4bc25f12dc01893916c53d393ecdc08d0/refinedstorage-common/src/main/java/com/refinedmods/refinedstorage/common/grid/AbstractGridContainerMenu.java#L212-L254)，只取設計模式、不照抄程式。

2026-07-21 typed-resource gotcha：Item、fluid、FE、chemical與addon kind的live amount現在只存在同一`CoreStorageRecord.resourceLedger`；`ItemKey`只作exact item bridge，legacy segmented item NBT只作save/load相容，禁止重建第二個runtime item map。Fluid identity必須保存exact components；NeoForge Energy是獨立`magic_storage:neoforge_energy`資源，絕不等同Fuel頁recipe energy pools。Capability每次call受`int`限制，內部amount仍是`long`。Mekanism只有`ModList`確認存在後才反射載入compat class；缺席時base dedicated server不可link任何Mekanism class，存在但註冊失敗則直接throw，不能靜默停用。Chemical持久化kind ID固定為0.1.31既有的`mekanism:chemical`；built-in registry以NeoForge alias連到`magic_storage:chemical`，不可改名或靜默遷移saved key。CI的`mekanism_ci_version`只指定代表性Modrinth artifact，release metadata不要求或exact pin Mekanism，也不建立多版本matrix；其他版本不相容由使用者回報後修正。Unknown kind/raw variant在provider缺席時照原key持久化，不可刪除；Terminal必須略過尚未註冊的kind，不能因找不到representative崩潰，也不能偽造fallback icon，provider恢復後才重新顯示。Storage view的server-owned selector固定依序切Item/Fluid/Energy/Gas/Other，Other只收未獨立分組且已註冊的addon kind；Craftable/Fuel必須隱藏並拒絕偽造的selector action。Typed代表物只能顯示，不得被取出、選成item recipe或送進EMI item source。

2026-07-21 Terminal resource selector GUI handoff：部署器已自動bump並部署唯一`magic_storage-0.1.32.jar`，build/deployed SHA-256同為`d185ac95963cf94047fd8f85a96a5f161f31b46fe79aaa0eb58c8ec625dec97b`。`terminal-left-rail` checklist已加入Items/Fluids/Energy/Gases/Other、反向切換、中鍵回Items及Craftable/Fuel隱藏檢查。兩次current-run啟動都在READY前被`glfwGetPrimaryMonitor failed`明確拒絕，artifacts為`build/gui-runs/20260721-225903-terminal-left-rail/`與`build/gui-runs/20260721-225920-terminal-left-rail/`；runner已清理兩次程序。這不是GUI verdict；喚醒並解鎖macOS顯示器後必須原樣重跑同一scenario。

2026-07-22 Bus config/security gotcha：現行`busConfig`是schema 2，schema 1 item rules仍可讀；schema 2另有exact non-item `resource` rule。舊Import預設directional/all sides/unsided enabled/Deny-empty，舊Export預設directional/all sides/unsided disabled/Allow + legacy exact filter。Unknown/malformed config、missing item/tag/mod/resource kind都保留raw並fail closed。Menu open payload只能傳`withoutOwner()` snapshot；server用container ID、position、expected revision、current BE identity、距離、spectator/mayBuild與owner/operator逐次驗證，stale menu變read-only，`Long.MAX_VALUE` revision update直接拒絕。Terminal held-container packet也必須在任何Core/cursor mutation前拒絕spectator與`stillValid=false`的遠距/失效menu；GameTest mock player若要測成功路徑，必須明確放到access block八格內。新放置記owner；legacy unclaimed只在第一次otherwise-valid menu/Wrench mutation由非operator claim，operator不claim。Drop保留config/filter/escrow但移除owner/runtime Core cache。Mode/side/unsided/automation變更後必須`invalidateCapabilities`；directionless模型由blockstate同步。

2026-07-22 Bus filter/transfer gotcha：Allow空規則=allow none，Deny空規則=allow all；duplicate只讓第一條ACTIVE，後續保留DUPLICATE；missing registry/raw rule保留UNAVAILABLE且match nothing。任何含UNAVAILABLE rule的Deny policy必須整體fail closed，不能因該rule「不匹配」而放行所有資源。Allow item candidates先依rule slot，再依item registry ID/canonical components；Deny依全域相同item順序。Canonical components必須用registry-aware codec與排序NBT，不依`toString()`/HashMap。Item rule不匹配typed key，exact typed resource rule不接受Item kind；generic typed capability也拒絕Item，避免繞過`IItemHandler` stack limits。Directional active front同一cooldown要**各自**嘗試一個item stack與一個bounded typed resource，成功item不可提前return而餓死typed；同時掃generic addon與全部native fluid/FE/chemical endpoints，generic不可遮蔽native。Import passive依side/null insert-only，Export只在directionless依side/null extract-only，item view只有一格且不暴露總量。Active item與typed transfer都必須用immutable plan保存Bus/config/Core/network/topology/path、endpoint blockstate/side/handler identity及actor，commit前與external execute callback後重驗；replacement endpoint不可沿用舊simulation。Import已抽出的stale item直接走current Bus recovery，Export先反向取回舊target已接受量，任何detached且不可取回的exact差額也必須顯式recovery，不能把「舊handler已吃掉」當作成功。Cached wrapper每次call重驗current BE、mode/side、automation、escrow、filter、loaded path與Core conflict；Import/Export缺Core都用10-tick negative cache，stable wrapper保持inert。所有路徑用structured `BusActor`與`BusTransferGuard`，包含escrow回流也必須保留Bus actor，不得退回unknown/system actor；recursive same-network/inverse call回0/empty。GameTest放置會自動tick的Bus fixture時，必須先寫入最終config/filter再開始延遲等待；否則legacy Deny-empty預設可在測試設定前搬空source，造成假失敗。

2026-07-22 Typed addon/escrow gotcha：公開`StorageResourceHandler`的resource list必須non-null、finite、unique、deterministic immutable snapshot；amount非負；insert/extract只接受positive request、wrong kind回0、回傳必在`0..requested`，simulate完全不mutation，execute精確mutation回傳量。Magic Storage會clamp/revalidate自己的邊界，但無法修復第三方handler在simulate偷偷mutation或謊報mutation；這是明確addon contract，不可silent guess。`StorageResourceBlockStrategy`不可load chunk且必須尊重exact side；generic與native endpoint可共存；同一次simulate→commit期間重取同一能力時，endpoint handler必須維持相同identity或stable equality，不能每次包成互不相等的throwaway wrapper。`StorageResourceContainerStrategy`每次收到獨立one-count stack copy，只能產生0/1 result container；一個strategy mutation不得污染下一個。Optional mod absent不可link class，present但binary-incompatible必須拋出含`binary-incompatible`的明確錯誤；反射method內拋出的`LinkageError`會包在`InvocationTargetException.cause`，也必須保留同一分類。Mekanism `IChemicalHandler#isValid(tank, stack)`只回答該tank的schema/type合法性，不得依目前內容或容量改變；指定tank的execute insert才另外要求existing chemical identity，generic insert可選擇可接受的新type tank。Typed plan在commit前及external execute callback後重驗Bus/config/Core/network/topology/path/blockstate/side/endpoint；Import endpoint在execute中被取代時不得寫回detached handler，已抽量全部進current Bus escrow/recovery；Export target被取代時先從舊target反向抽回可逆accepted量，再回Core，無法立即保存的部分才進escrow/recovery。若callback已移除/取代Bus，禁止寫入detached BE escrow，必須產生owner-stripped recovery drop。pending時cached/fresh passive capabilities全inert，tick在automation/mode gate前回流Core。normal/Wrench/Creative no-drop/explosion/direct air或其他block replacement拆除都先recover或把escrow保存在單一owner-stripped recovery drop；每個Bus instance都有runtime recovery UUID並寫入drop，HIGHEST-priority handler會在其他drop listener之前把含escrow的Bus從event取出並直接保存；emergency recovery不受event取消或`doTileDrops=false`影響，同位置同tick的兩個不同Bus不會被誤判重複，而同一Bus的loot-first/`onRemove`仍只保存一份。Wrench event被取消時plain Bus硬體也必須保留。無法保存就拒絕拆除。Wrench legacy claim一定在conservation preflight後，no-op rotation不claim，失敗不可改owner/revision；unknown kind/raw future NBT保留且Bus fail closed。Escrow add先full simulate再commit，`long` overflow不得partial mutation。

2026-07-23 Bus post-audit gate：在既有0.1.34部署後再以RED→GREEN補齊同cooldown item+typed並行、Import/Export execute-time endpoint replacement、no-Core negative cache、stale config/topology/callback移除Bus、`doTileDrops=false`與loot-first/direct-air escrow conservation、failed/no-op legacy Wrench claim、Mekanism exact-tank identity、wrapped optional-mod linkage分類、Terminal cursor transaction batching、滿背包拒絕及spectator/失效menu packet authorization。第二輪review再以四個精確RED重現：detached Export target拒絕反向reclaim會吞掉已接受item、同位置同tick兩個同escrow Bus被錯誤dedupe、後續`BlockDropsEvent` listener可攔截/取消escrow drop，以及取消Wrench drop event會失去plain Bus；production加入active item immutable plan、exact unreclaimable差額recovery、per-Bus recovery UUID與HIGHEST-priority escrow event protection。`TypedResourcePersistenceTests`必須保持`@GameTestHolder(MagicStorage.MODID)`；曾改成自訂namespace會讓11個測試只編譯不執行，post-run count因此從預期363掉到352。該批base gate固定為371。完整CI等價GREEN為`build`、EMI minimum/latest-compatible compile（本次同為`1.1.24+1.21.1`/`5sIPA1To`）、SelfTest 224723/224723、base GameTest 371/371、recipe-addon 14/14、Mekanism 5/5、Python 217/217、Fusion/EMI/Patchouli-backed`runData` 0 files written/no resource drift與`git diff --check`。完整gate初次重跑另抓到兩個新Bus測試在`runAfterDelay`前跨tick關閉全域`doTileDrops`，使並行Wrench測試假紅；已把gamerule切換縮到同步callback並以`finally`還原，之後base 371/371重綠。部署器已依約只執行一次並自動升至0.1.35；build/deployed SHA-256同為`639160a638a9b614f23abe751b6ffd5f6374b8c0bc45f3c30fd860bfff7b4a67`，Prism dev mods內只有`magic_storage-0.1.35.jar`與釘選Fusion。`bus-configuration` visual scenario使用hotbar 5/6檢查共享Import/Export設定、Front/Pipes、六面、unsided/automation/filter與directionless models；READY與使用者fullscreen verdict仍須另記，automated gate不可代替。

2026-07-23 polymorphic station / optional recipe gotchas：同一logical descriptor可有1–64個exact item variants，每個variant保存normalized rational work rate；Core的fractional remainder同時綁variant item ID與rate，換方塊或live config值改變時必須重置不相容remainder，不能沿用舊速率。第三方config holder可在registry階段取得，但值只能在config載入後透過variant rate supplier延遲讀取；禁止在DeferredRegister callback直接`get()`。Recipe preview由server同步完整variant snapshot，actual installed stack固定排第一，其餘依descriptor順序，每40 ticks只在client輪播；執行與速率永遠不讀client index。部分第三方public recipe classes（Mekanism即為例）即使是exact deterministic contract也回報`isSpecial()`；無predicate的`deterministicResources`仍fail closed，只有帶explicit eligibility predicate的overload可明確接受，不能全域放寬special recipe。Mekanism Pressurized Reaction的`energyRequired`是每tick額外FE，總typed input必須checked計算為`energyRequired × duration`，overflow就不支援，不能把per-tick值誤當整份配方總量。Optional compat只在`ModList`確認mod存在後反射載入；不存在不link class，存在但API不相容直接以具名binary-incompatible錯誤停止，禁止silent fallback。

2026-07-23 polymorphic station / optional recipe final automated gate：`./gradlew build`、Modrinth EMI minimum/latest-compatible compile（本次皆`1.1.24+1.21.1`/`5sIPA1To`）、SelfTest 224741/224741、base GameTest 372/372、recipe-addon 17/17、Mekanism 17/17、Iron Furnaces 3/3、Farmer's Delight 5/5、Python 218/218、Fusion/EMI/Patchouli-backed `runData` written 0/no resource drift與`git diff --check`全部通過。五組GameTest每次都先刪`run/world`並依序執行；不可平行跑或共用舊world。這批只完成automated acceptance；依使用者要求未啟動Prism，recipe station preview輪播與Fuel-page variant外觀仍留待所有issue完成後一次fullscreen目視。

2026-07-17 Smithing variant gotcha：exact concrete `SmithingTransformRecipe`與`SmithingTrimRecipe`都必須從目前Core/可選玩家背包的完整component stacks解出source-bound contracts，不能讓generic `resolveVariants`用「空inputs的presentation是否非空」決定支援。每個bound contract的template/base/addition predicate同時驗原recipe predicate與exact `ItemKey`，輸出再呼叫目前recipe的`assemble`核對identity/count。Trim同一recipe ID可產生多個exact outputs；lookup/select/current/reload/execute必須以selected output identity重解current holder variant，commit不可退回只按ID的base match；若呼叫只提供ID且目前有多個variant，必須fail closed，不能猜canonical第一個。來源先按item registry ID + registry-aware canonical component NBT排序，不能依Core/hash/source iteration；template×base×addition超過65,536、component codec失敗、trim material/pattern mapping缺失或assemble為空時整體fail closed，不可回傳partial variants。Transform只有generic result lookup可顯式用recipe target identity找到canonical source-backed variant以保留既有操作；一旦已有exact selected output，所有後續路徑仍只能exact match。Special/dynamic/incomplete subclasses保持neutral unsupported。

手動測 GUI 優先用 session runner；本次 Fuel 頁使用 `python3 scripts/run_prism_gui_session.py --scenario crafting-fuel-page`。runner 會把 Prism dev `instance.cfg` 設成 `WrapperCommand=`、`OverrideConsole=true`、`ShowConsole=false`、`ShowConsoleOnError=false`，transactionally 重建固定 world，將 `options.txt` 設成 Minecraft 內建 fullscreen，並先要求一般Prism Launcher已開啟且account initialization完成，再直接執行 `"/Applications/Prism Launcher.app/Contents/MacOS/prismlauncher" -l dev -w MagicStorageGuiTest -o MagicStorageBot`把launch要求送給該warm process。若找不到normal-root Prism process，runner會在改world/啟client前fail，避免`-o`冷啟動仍觸發Microsoft/Xbox ownership refresh。此命令使用一般Prism root與既有dev instance，不建立`-d` root、不建立/改寫`accounts.json`；`-o`只指定本次Minecraft離線玩家名稱，Prism仍使用既有owning account確認完整遊戲資格。Minecraft 會自動進入 Minecraft F11 fullscreen；runner分別記錄normal-root launcher log與Minecraft log cursor，等到 `SelfTest:` + `MS_GUI_TEST_READY` 後只掃本次launcher片段。出現`AuthFlow:`實際step或Microsoft/Xbox/XSTS/Minecraft-services endpoint就fail closed並清本次process；只有generic Offline `AuthFlow(...)` task與`RefreshSchedule` bookkeeping不算網路登入。通過後才輸出 `manual_handoff_message` 與 `build/gui-runs/<timestamp>-<scenario>/checklist.md`。到這裡自動化立即停止；Visual verification owner: user。使用者只需確認全螢幕 gate 後執行 checklist，不再手動切換模式。GUI 外觀無法用 GameTest/client-smoke 驗證。

### Prism dev / manual handoff

預設流程不再由 agent 用 Computer Use 逐步操作 Minecraft GUI；runner 只負責確認normal Prism已warm、準備、用官方CLI把既有dev instance交給該process啟動、等 READY 與收集本次 log，之後直接請使用者接手。這避免 LWJGL click/文字輸入與 Accessibility/Screen Recording 權限造成長時間等待。runner 直接執行明確的app binary path，不透過LaunchServices display name、不用`open -n`、不另開 Terminal.app；launcher subprocess使用最小必要環境，artifact會移除Prism列出的process/native environment，避免把agent secrets保存進log。若 agent 為診斷而開了額外 Terminal 視窗，流程結束前只關閉自己建立的視窗，不可動使用者原有 Terminal；結束時也要確認沒有自己留下的 Prism/Minecraft/Gradle process。使用者仍在手動目視時不可清掉 active client；只有使用者表示結束/卡死，或 log 已記 `Stopping!` 但 Java 仍殘留時，才先 TERM、逾時再 KILL。

**不要把`-o MagicStorageBot`解讀成「不需要正常Prism帳號」。** Prism 11.0.3的官方CLI將`-o/--offline`定義為本次launch的離線玩家名稱；上游`LaunchController`仍會尋找擁有Minecraft的account確認完整遊戲資格，找不到就進Demo。因此2026-07-22嘗試的單一Offline帳號`-d` root會卡在Demo/account-selection，已明確禁止。現行runner使用已經開啟並完成初始化的normal-root Prism process、既有owning account與dev instance，但不改`accounts.json`。Prism每次cold start都把持久化account載入為refreshable，`-o`只控制game launch而不關閉startup account queue；所以runner在沒有warm process時直接fail，不再偷偷冷啟動。READY前仍對本次launcher片段做fail-closed auth endpoint掃描。`RefreshSchedule`或generic `Task "AuthFlow(...)"`本身可能只是Offline本地狀態更新，不可誤判；真正`AuthFlow:` step與Microsoft/Xbox/XSTS/Minecraft-services URL才算失敗。Vanilla authlib仍可能對離線UUID嘗試抓skin/profile properties，並在`latest.log`留下`Failed to fetch user properties` / 401 stack；這不是Prism帳號登入或token refresh。runner只針對這一段完整已知Minecraft stack白名單，其他current-run `ERROR` / `FATAL` / `Caused by`仍fail。參考官方 [Prism CLI](https://prismlauncher.org/wiki/getting-started/command-line-interface/)、[Prism Launcher 11.0.3 release notes](https://prismlauncher.org/news/release-11.0.3/)、[11.0.3 CLI parser](https://github.com/PrismLauncher/PrismLauncher/blob/8ae894c7b1f74a58405a2dabd5614dae4ef3f08c/launcher/Application.cpp#L310-L339) 與 [offline launch decision](https://github.com/PrismLauncher/PrismLauncher/blob/8ae894c7b1f74a58405a2dabd5614dae4ef3f08c/launcher/LaunchController.cpp#L138-L200)。

若 current-run 在 NeoForge early display 出現 `Failed to find a primary monitor` / `glfwGetPrimaryMonitor failed`，代表當下 macOS 沒有向 GLFW 暴露 active monitor，常見於螢幕睡眠或鎖定；`caffeinate -u` 在仍鎖定時也不保證可解。runner 會用明確的「wake and unlock」錯誤 fail closed 並清理本次 processes，不可把這段 `ERROR` 白名單化。使用者先實際喚醒並解鎖顯示器，再重跑同一 scenario。

2026-07-17 out-of-chunk Core storage revision：NeoForge custom registry仍為`magic_storage:machine_descriptor`，第三方以`MachineDescriptorApi.createDeferredRegister(modId)`在正常registry lifecycle註冊；server凍結/cached ordered snapshot、Crafting/Remote opening payload、clientbound finite/∞ state與固定256-slot bank不變。Core永久payload從建立時起由overworld `magic_storage_core_storages` SavedData的`CoreStorageRepository`唯一擁有，`StorageCoreBlockEntity` chunk NBT只寫`storageId`/`storageSchema`。`CoreStorageRecord`保存network UUID、完整component ItemStack+long count、energy、machines、descriptor state與raw unresolved entries；inventory以每段最多63種、任意多段保存，63不是gameplay容量。含資料Core拆除只建立recovery UUID→同一storage UUID的mapping，token不含`BLOCK_ENTITY_DATA`且claim不複製payload；`/magic_storage recover_core`重發同一未claim capability。missing/corrupt/packed/duplicate attachment全部fail closed並顯示localized unavailable訊息；corrupt/orphan raw NBT會記log後原樣重寫。舊inline Core NBT、舊`magic_storage_core_recovery` snapshot、legacy bottle/slot/axe格式刻意不遷移。第三方API見`docs/machine-descriptor-api.md`。

2026-07-17 GitHub #8 Creative Storage Unit revision：第一個production edit前先新增`CreativeStorageUnitTests`並以`compileJava`得到預期`cannot find symbol: StorageTypeCapacity` RED。Production現在用`StorageTypeCapacity(finiteTypeSlots, unlimited)`明確建模種類容量，不以`Integer.MAX_VALUE`或加法sentinel表示；有限值仍保留並checked composition，任一有效Creative Unit把結果切為unlimited，多個不重複放大。Core full rebuild與incremental add共用相同capacity resolver；currently-loaded bounded BFS與multi-Core conflict不變。insert與craft output capacity planning只在新`ItemKey`時查explicit predicate；移除、斷線、chunk unload、Wrench與爆炸不動Core inventory，有限容量不足時沿用既有over-capacity行為。Storage base menu新增最後一個unlimited bit，parity為12；Crafting再加89、總101，open menu依topology revision即時刷新。Creative Unit已註冊block/item/creative tab/command ID、loot/pickaxe tag與雙語名稱/tooltip，tooltip明示無限種類且不生成物品；刻意無recipe且無BlockEntity/庫存/tick。Fuel capacity在unlimited時畫Creative Unit item，finite仍畫T1。資產以login-zsh載入既有Replicate credential、`retro-diffusion/rd-fast` seed 71425與shared chassis img2img產生三個候選，選candidate 01後沿既有palette/chassis/symmetry semantic cleanup完成16×16 infinity cell、Fusion 80×16 `pieced` sheet/model/mcmeta、12-block predicates、portable metadata/manifest與contact sheets；runtime metadata不含credential。Prism deterministic lab manifest schema 4在active network加Creative Unit，isolated/connected galleries各加一個並在player inventory供應item；`crafting-fuel-page` checklist現在覆蓋雙語∞、Fuel Creative icon、tooltip、native/Fusion外觀。自動lab/static tests已綠，但本次不啟動Prism，current fullscreen manual verdict仍待使用者完成。Chunk unload GameTest選遠端chunk時不能只檢查它不在`getForcedChunks()`；forced chunk的FULL ticket仍向外傳播兩格，候選必須在所有forced chunk的Chebyshev距離2之外，否則測的是GameTest ticket而非storage lifecycle。

2026-07-15 Fuel full-height/multi-row correction 已完成 focused RED→GREEN、完整 automated gates、0.1.29 部署與 current-run handoff。RED先以64個Consumables/Timed/Instant descriptors、61個targets與inventory-side status geometry鎖定舊版固定高度置中、single-row flow、Instant status reservation；GUI checklist test另先因舊版仍要求`compact stored/max value above the player inventory`而失敗。GREEN讓三個category panels由`TOP_HEIGHT`平均填滿到Inventory label band，`FlowGrid`依實際寬高使用columns × rows並保留panel-local paging，Instant Stations取得完整寬度，完整localized `stored / max types`則在player inventory正右方的獨立panel顯示。Final為Python 173/173、`build`、`runGameTestServer`（SelfTest 224642/224642、GameTest 269/269）、Fusion/EMI/Patchouli-backed `runData` no-status-drift與`git diff --check`。部署器bump至0.1.29，build/deployed SHA-256同為`7bd6ac6d9e128892289bddb40a4e4134f3a209ae14e7310fb10298d3b26ccf42`，Prism mods只有唯一Magic Storage 0.1.29與釘選Fusion/EMI/Patchouli。`build/gui-runs/20260715-143727-crafting-fuel-page/`已到`MS_GUI_TEST_READY`，log為SelfTest 224642、Patchouli 46 JSON，唯一ERROR/Caused-by是精確白名單的offline profile-properties 401。使用者其後明確回報該0.1.29 Fuel GUI通過；但Minecraft仍刻意保持開啟，F11→windowed→Command-Q與shutdown artifact尚未完成。後續descriptor/Remote title改動屬新GUI版本，不能沿用這次visual verdict。

2026-07-12 15:27 已驗證上述恢復路徑：使用者解鎖後，同一 `crafting-fuel-page` offline command 成功載入 `magic_storage-0.1.8.jar`、SelfTest 117、Patchouli 17 entries 與 `MS_GUI_TEST_READY`，runner 保留 client 並交給使用者做 fullscreen checklist；run artifact 在 `build/gui-runs/20260712-152647-crafting-fuel-page/`。後續使用者回報的目視/互動結果是 **0.1.8 不通過**：Cooking Energy 應叫 Fuel、原木被 Fuel input 拒絕、recipe cost 不應硬編碼、Craftable 無法顯示零庫存木炭、GUI 固定座標/暗色面板仍不美觀。Active replacement plan：`docs/superpowers/plans/2026-07-12-fuel-craftable-emi-adaptive-ui.md`。

2026-07-12 replacement implementation 已完成 RED→GREEN：runtime Fuel、concrete cookingtime、zero-storage Craftable catalog、EMI exact amount/destination one-level craft、original semantic slot delegates、vanilla-like palette 與 EMI exclusions。0.1.9 fullscreen review 的功能面通過（Charcoal craft 成功），但 GUI gate 失敗：完整 frame 在 guiScale 4 幾乎貼齊 viewport、recipe pane 只顯示 Ready 而沒有現有/單次需求、Compact Grid 無清楚用途、Unicode rail glyph 大小不一；因此 0.1.9 不可標記 GUI verified。Task 6 已再以 RED→GREEN 完成：右 pane 同步完整 ingredient/process/Fuel `Available / Required`、完整移除 Compact、rail 使用 12×12 fixed-canvas icons、layout 依完整 rail+frame+margin geometry 自動選 narrow/side-by-side。`compileJava`、`build`、SelfTest 39823、GameTest 205、Python 68/68、`runData`、77 JSON parse 與 diff/drift checks 全綠；deploy script 自動 bump/部署 0.1.10，Prism mods 只有 `magic_storage-0.1.10.jar`，build/deployed SHA-256 同為 `2697c631ead5351af5bed7803656f1b1a80f3c0aed83e496e2a8087090afd59b`。只剩新版本使用者 fullscreen GUI gate。

2026-07-12 21:50 已啟動 0.1.10 `crafting-fuel-page` manual handoff：artifact 在 `build/gui-runs/20260712-215025-crafting-fuel-page/`；`launch_profile` 證明 native offline `MagicStorageBot`、Computer Use wrapper disabled、error-console pop-up disabled。current-run log 已看到 Magic Storage 0.1.10、SelfTest 39823 與 `MS_GUI_TEST_READY`。其中仍有唯一白名單的 authlib profile-properties 401（skin/profile property fetch，不是 Microsoft/Xbox login/token refresh）；runner 對其他 `ERROR`/`FATAL`/`Caused by` 仍 fail closed。Minecraft 刻意保持開啟供使用者完成 fullscreen checklist。

2026-07-12 0.1.10 fullscreen 結果：**失敗，不可標記 GUI verified**。實機圖確認 rail 的 page/navigation 與 sorting/search controls 沒有群組間隔；Craftable 仍藏在 Items toggle；Fuel panel 被塞在 recipe workspace，固定五個 machine slots/三個 reserves，顯示多餘 `stack = energy/t`，machine/reserve 的 `drawCenteredString` 黑色 shadow 降低辨識；×1/×8/×64 不依 live craftable count 變暗，無 Max；search mode 仍把 `#`/mod 標記疊在 magnifier 上。Task 7 contract 已改為 Storage/Craftable/Fuel 三頁、full-width count-driven Fuel flow、shadowless values、server Max、live-disabled exact buttons、semantic magnifier/`#`/`@` icons。下一次只可用新 jar 重跑 fullscreen checklist。

2026-07-12 Task 7 已依 RED→GREEN 完成：`CraftingTerminalPage` 僅有 Storage/Craftable/Fuel，Craftable synthetic slots 不可抽取且不再作 EMI input source；Crafting extra data 84→83、總 parity 95→94；ID 5 改為重新解析 recipe 並計算 fresh server maximum 的 Max，wire/display preview 仍安全限制 9,999，但 Max 本身不受該上限截斷；client 依 live `craftableCount` dim ×1/×8/×64/Max。`TerminalLayout.FlowGrid` 由 descriptor counts 計算 Fuel cells/capacity/page，Fuel 使用完整 inner frame width與 panel-local wheel paging；左 rail 固定為三個 page tabs 加 Auto/previous/current-next target navigator，不再隨 reserve 數量水平膨脹。rail group gap、shadowless values、hover details、magnifier/`#`/`@` icons 均有 SelfTest/Python guards。Automated結果為 SelfTest 40321、GameTest 208、Python 69，build/runData/77 JSON parse 全綠且 datagen 無 drift。0.1.11 同版本最終 jar 已 transactionally 重新部署，Prism mods 只有 `magic_storage-0.1.11.jar`；build/deployed SHA-256 同為 `09fd5ca4e2b2d41c1163daa255da58232be021f1a131f30e53d80748f37b6d1d`。只剩新的使用者 fullscreen GUI gate，不可先宣稱目視通過。

2026-07-12 23:32 已啟動上述 final 0.1.11 `crafting-fuel-page` manual handoff，artifact 在 `build/gui-runs/20260712-233238-crafting-fuel-page/`。`session.json` 證明 native offline `MagicStorageBot`、Computer Use wrapper disabled、error-console pop-up disabled；current-run log 已載入唯一 `magic_storage-0.1.11.jar`、SelfTest 40321、Patchouli 17 entries、EMI plugin 與 `MS_GUI_TEST_READY`。唯一 ERROR/Caused-by 是 runner 明確白名單的 offline profile-properties 401，不是 Microsoft/Xbox login/token refresh。Minecraft 刻意保持開啟，等待使用者先切 fullscreen 再執行 checklist；尚未取得 visual verdict。

2026-07-12 0.1.11 fullscreen 結果：**失敗，不可標記 GUI verified**。使用者確認 Energy Reserves 的抽象 glyph 不如指標性物品直觀、grid 的長數量文字會向左侵入相鄰物品、Fuel 頁缺少目前 stored types / total type capacity。Task 8 先對照 RS2 develop `0643e6f`：`ItemGridResource#getDisplayedAmount` 先產生 compact unit text，`ResourceSlotRendering#renderAmount` 再以 16px 門檻選正常或 0.5 scale，兩者都在 slot 內右對齊。Magic Storage 只採此模式並自行實作：`TerminalAmountFormatter` 採不高估的 K/M/G/T/P/E compact 值，超過 16px 的文字縮半並在自己的 16px cell 內 right-align；全部八種能量用對應 machine/fuel item 作代表物，explicit target 也顯示相同代表物；Fuel 頁沿用既有 base 11 data slots 顯示 stored/max types，不新增 client storage state。RED 證據為 missing formatter/representative API 的 13 個 compile errors，以及兩個 Python contracts 失敗。額外 GameTest 再以 `menu=10/core=35` 證明 open Fuel page 的總容量會在 topology 成長後停留舊值，production 改為 topology revision 一變就 refresh display metadata，並同時守住 type 插入/最後一個 type 移除。最終 SelfTest 40340、GameTest 209、Python 70/70、build、runData、77 JSON parse 與 datagen no-new-drift 全綠；deploy 自動 bump 至 0.1.12，Prism mods 唯一 `magic_storage-0.1.12.jar` 與 build jar SHA-256 同為 `f7b0e78e630c03f77331fa9aad4c4d3f04512d069b327c9bad6942a0c8844309`。只剩新版本使用者 fullscreen GUI verdict，不可先宣稱目視通過。

2026-07-13 00:12 已啟動 0.1.12 `crafting-fuel-page` manual handoff，artifact 在 `build/gui-runs/20260713-001205-crafting-fuel-page/`。`session.json` 證明 native offline `MagicStorageBot`、Computer Use wrapper disabled、error-console pop-up disabled；current-run log 已載入唯一 Magic Storage 0.1.12、SelfTest 40340、Patchouli 17 entries 與 `MS_GUI_TEST_READY`。runner 只接受已知 offline profile-properties 401，其餘 forbidden patterns 未觸發。Minecraft 刻意保持開啟供使用者先切「好全螢幕」再檢查代表物、slot-local compact amount 與 Fuel stored/max types；visual verdict 尚未回報。

2026-07-13 0.1.12 fullscreen 結果：**失敗，不可標記 GUI verified**。使用者截圖已保存為 `build/gui-runs/20260713-001205-crafting-fuel-page/user-feedback-fuel-layout.png`。實機證據顯示 Fuel page 仍有兩個結構問題：`FlowGrid` 以 panel 可容納的最大欄數切格，只有五台 machine/三種 reserve 時內容全部靠左而留下大片無功能空白；target rail 把 previous 永久畫成箭頭，卻把 next 在 explicit target 時改畫「目前 target」的物品，造成按鈕顯示的是狀態、按下執行的卻是 next action。Task 9 改採 vanilla `CycleButton` 作單一 current-value selector，放在 Energy Reserves 標題列；Fuel rail 只留 Storage/Craftable/Fuel page tabs，普通點擊向前、Shift-click 或 hovered scroll 反向，server 仍只接受 Auto 或 exact target button ID。flow cells 改依目前頁面實際 descriptor 數平均鋪滿 bounds，超過可視上限仍 deterministic paging。若未來 runtime target descriptors 多到 cycle UX 不適合，再把同一 selector rectangle 升級成可捲動選單；不可重新把每個 target 塞回 rail。

2026-07-13 Task 9 已依 RED→GREEN 完成，final artifact 部署為 0.1.14。RED 先由 SelfTest 缺少 `fuelTargetSelector`/`fuelRailPanel` geometry 產生 5 個 compile errors，三個 static contracts 與 Prism checklist 失敗；完成 client layout 後，新增 GameTest 再單獨證明舊 button ID `17` 仍會改掉 Blaze target，210 tests 中只此一項失敗。Production 改為 Energy Reserves header 單一 vanilla `CycleButton`，Fuel rail 只畫三頁籤；普通點擊向前、Shift-click/scroll-up 反向，selector hovered wheel 優先於 reserve paging。Server 移除 previous/next IDs 與 `cycleFuelTarget`，只保留 Auto/exact target IDs，94 data-slot parity 不變。`FlowGrid.cells(page)` 依每頁可見數量計算比例邊界，稀疏第一頁與 partial final page 都精確碰到左右 bounds。最終 `compileJava`、`build`、SelfTest 40406、GameTest 210、Python 70/70、`runData`、77 JSON parse 與 datagen no-new-drift 全綠；0.1.13 intermediate 在最後加強 retired-ID assertion 後由 deploy script 的 patch bump 取代，Prism mods 現在唯一 `magic_storage-0.1.14.jar`，build/deployed SHA-256 同為 `97069ca985797ce237b421df2fc5c08c44cc2ca4dedf1957d0f756c2c92dae75`。只剩 0.1.14 使用者 fullscreen GUI verdict，不可先宣稱目視通過。

2026-07-13 04:55/04:55 兩次嘗試啟動 0.1.14 `crafting-fuel-page` manual handoff，都在 READY 前被 NeoForge early display 的 primary-monitor gate 明確拒絕：`Minecraft could not access a primary monitor; wake and unlock the macOS display`。只產生 `build/gui-runs/20260713-045509-crafting-fuel-page/` 與 `build/gui-runs/20260713-045526-crafting-fuel-page/` 的 manifest/checklist，沒有 `session.json`、沒有 GUI verdict。runner 已清理兩次啟動的 Prism/Minecraft/Gradle processes；agent 沒有開 Terminal.app，系統原有 Terminal 視窗不可代替使用者關閉。使用者需先喚醒並解鎖實體顯示器，再原樣重跑同一 scenario。

2026-07-13 station/recipe/output/texture revision 已完成 focused RED→GREEN：Core equipment 5→9，新增 Crafting Table/Stonecutter/Smithing Table/axe station gates；Installed Stations 改為兩個 flow rows，wide Fuel 的右下區改由 `fuelControlPanel` 容納 target selector/Fuel input；recipe workspace 使用 server-synced input→operation→output + Available/Required grammar，不嵌 EMI 私有 screen。可執行範圍新增 exact component-matched `SmithingTransformRecipe` 與只掃 vanilla namespace default state 的 axe strip/scrape/wax-off synthetic recipes；axe 每 craft 固定耗 1 raw durability，trim/dynamic/context-only/mod-block hooks 仍 fail closed。Inventory destination 先裝玩家 36 格，overflow 回 Core，兩邊都放不下就整筆 no-op；不存在 requested-output world drop/void path。Max 以 long-safe 資源上限與 destination plan 找出最大完整可交付數，且跨 `Integer.MAX_VALUE` Core reservation/output 另有 GameTest 守護。所有 model-referenced textures 已改成 16×16，生成 metadata/preview 留在 `art/texture-generation/20260713-cohesive-16x16/`。0.1.15 final automated gates 已全綠：build、SelfTest 40608、GameTest 222、Python 72/72、runData、JSON parse 與 datagen no-drift；使用者本輪要求先 commit/push，因此 Prism exact-jar deployment 與 fullscreen verdict 明確延後，不可先宣稱 release 或 GUI verified。當時 contract：`docs/superpowers/plans/2026-07-13-stations-recipes-output-textures.md`；現行 replacement 見 2026-07-14 shared-terminal plan。

2026-07-13 21:24 已完成 0.1.15 current-HEAD audit 與 `crafting-fuel-page` manual handoff。`build`、SelfTest 40608/40608、GameTest 222/222、Python 72/72、`runData`、77 JSON parse、datagen no-drift 與 exact-SHA GitHub CI 均通過。Prism mods 內唯一 jar 為 `magic_storage-0.1.15.jar`，build/deployed SHA-256 同為 `70cf4301a692614d052901c47e9bd65b5975b34d344636c43ed206691bc9e6f8`；舊 0.1.14 已備份至 `~/Library/Application Support/PrismLauncher/instances/dev/minecraft/magic_storage_backups/20260713-212427-892849/`。runner artifact 在 `build/gui-runs/20260713-212436-crafting-fuel-page/`，`session.json` 證明 native offline `MagicStorageBot`、wrapper/error-console disabled；current-run log 已載入 0.1.15、SelfTest、Patchouli 17 entries 與 `MS_GUI_TEST_READY`。唯一 ERROR/Caused-by 是 runner 精確白名單的 offline profile-properties 401，不是 Microsoft/Xbox login/token refresh。Minecraft 刻意保持開啟，等待使用者先完成 fullscreen gate 再執行 checklist；尚未取得 visual verdict，不可標記 GUI verified。

2026-07-13 22:14 true-void lab replacement 已完成 RED→GREEN 與實機 handoff。新增/更新 Python contracts 鎖住 exact flat-air void codec、spawn `(0,80,7)`、15 類 copied runtime state strip、嵌入 `level.dat/Data/Player` 移除、來源不變/target rollback、37×25 lab、Core/station/energy NBT baseline、deterministic player slots、hotbar `1`–`9`、3-tick ready/reset latch 與 current `Installed Stations` checklist；Python 76/76、`build`、SelfTest 40608/40608、GameTest 222/222、`runData`、datagen no-drift 全綠。第一次 artifact `build/gui-runs/20260713-220829-crafting-fuel-page/` 因 sleeping display 的 `glfwGetPrimaryMonitor failed` 在 READY 前明確失敗且 runner 清理本次 processes；wake 後先證明新 lab 可 READY，再補掉 singleplayer embedded Player 邊界並產生 final `build/gui-runs/20260713-221433-crafting-fuel-page/`。final current-run log 有 SelfTest、Patchouli 17 entries、`MS_GUI_TEST_READY`，只有精確白名單的 offline profile-properties 401。另抓到 21:24 handoff 遺留、已 reparent 到 PID 1 的舊 `org.prismlauncher.EntryPoint` 並精確關閉；目前只保留 22:14 的 Prism/Minecraft 給使用者 fullscreen 目視，仍不可先標記 GUI verified。

2026-07-14 shared-terminal Task 2 已依 RED→GREEN 完成：dist-neutral SelfTest 先因缺少 `TerminalProfile`、`TerminalCycleDirection`、sort/search `previous()` 與 fixed-scale API 產生 41 個 compile errors；subagent 新增的 focused static contracts 再以唯一 failure 指出 Fuel target 仍使用 vanilla `CycleButton`。Production 現在只有 `TerminalLayout.forProfile(...)` 一個 layout entry，Storage 是 reduced profile、Crafting 以 capability hook 加 pages/source/recipe/Fuel 而不 hide/recreate common widgets；shared shell 擁有 common rail、focus cleanup 與 amount renderer。Rail controls 固定 18×18、16×16 semantic-item/`terminal_controls.png` atlas；所有 cycle controls（含 Fuel target）統一 left-next/right-previous/wheel-down-next/wheel-up-previous，tooltip 只列目前值與 hint；grid amounts 使用一個 screen-wide scale。`compileJava`、SelfTest 40999/40999、GameTest 223/223、focused Python 37/37、full Python 84/84 已綠。這是 automated gate，不是 fullscreen GUI verdict；Task 3 exact recipe presentation 尚未開始。

2026-07-14 shared-terminal Task 3 已依 RED→GREEN 完成：首批 5 個新 GameTest 先以 28 個缺少 `RecipePresentation`/kind/getter 的 compile errors 證明 exact server model 尚不存在；43 個 focused static tests 再以 4 個 failures 鎖住 client model consumption、exact output count、typed ledger palette 與 diagram→ledger→footer geometry；dist-neutral layout tests 亦先因新 geometry API 缺失產生 31 個 compile errors。提交前再加入 3 個契約 GameTest，精確重現超過 3×3 shaped recipe 未 fail closed、同類 recipe 未以 exact ID 穩定排序，以及 exact navigation 後 reload 仍驗證舊 ID；最小修正後連同既有一般選取移除契約全綠。Production 現在從 active server `RecipeHolder` 建立 exact ID/kind、shaped dimensions/positions、shapeless marker、精確 output components/count、station、aggregated item rows 與 distinct energy/tool rows；22 個 hidden slots 加既有 94 data slots同步，client `CraftingTerminalScreen` 不掃 `RecipeManager`/Core。Native workspace 不再切固定九個 resource cells，而是 positioned diagram、0..12 動態 ledger cells、footer；item row 使用 neutral palette，energy/tool 使用 dark-red palette，tooltip/hit test 與 rendering 共用 geometry。Final gate 為 `compileJava`、`build`、SelfTest 222946/222946、GameTest 231/231、focused Python 43/43、full Python 90/90、`runData` 且 datagen no-new-drift。這仍不是 fullscreen GUI verdict；當時的下一步 Task 4 EMI public-widget renderer 已由下方紀錄完成。

2026-07-14 shared-terminal Task 4 已依 RED→GREEN 完成：5 個 focused static failures 先證明缺少 full EMI dev runtime、renderer boundary、public-widget adapter、exact selection 與 bounded/no-catch contract。Production 新增無 EMI import 的 `RecipeDiagramRenderer`/native implementation；`ClientSetup` 只有在 `ModList` 確認 `emi` 後才進 guarded bootstrap，EMI plugin register 先標記 registry ready。Exact standard recipe 由 client EMI manager 以 server-synced ID 找到並要求 matching backing holder，再把 `EmiRecipe#addWidgets` 填入 bounded public `WidgetHolder`；render、mouse/key 與 tooltip 都限制在 diagram bounds，沒有 `WidgetGroup`、recipe screen、`EmiRenderHelper` 或 broad catch。無 EMI 及 internal synthetic axe 是當時的 native capability fallbacks；ledger/footer仍由 Magic Storage 擁有。`runtimeOnly` 讓 dev client 載入 full EMI；當時 release metadata 尚 optional，已由 2026-07-15 的 client-only required `[1.1.24,2)`政策取代，jar仍不bundle EMI。本機 Terraformers full-jar GET 曾持續截斷；經使用者明確同意後，以同一官方 URL 的 64 KiB byte ranges 重組，驗證 1,111,538 bytes、ZIP entries、SHA-1 `3fd494b37027dfbc7f78414d7cbf251c9eb6738a` 與 SHA-256 `b68691f94f0727fc517cac2ab55bd6658d1179cc8cc715fe610b60c37469bc4c` 後只填 Gradle cache，未改 repo/CI source。GREEN 為 `compileJava`、`build`、SelfTest 222946/222946、GameTest 231/231、focused Python 48/48、full Python 95/95、`runData` no-new-drift 與 `runClient --dry-run`；fullscreen EMI widget verdict 仍留到整批最終 gate。

2026-07-14 shared-terminal Task 5 已依 RED→GREEN 完成：GameTest 先以 34 個預期 missing `TerminalOutputDestination`/button/getter compile errors 鎖住獨立 server session state、Player/Core routing、Storage-only primary+remainder、exact-capacity/one-short no-op、四種 direct amount、EMI authority、source independence、rollback、long boundary 與 95-slot wire parity；兩個 focused static failures再鎖 shared rail/profile 與雙語 tooltip。Production 用內部 `DeliveryTarget` 明確分流 EMI request destination 與 direct-terminal toggle；預設 Player 在扣完 player-source ingredient 後 inventory-first/Core-overflow，Storage 則 primary outputs 與 crafting remainders 全部只進 Core。所有輸出 merge/capacity 在 mutation 前 long-safe 驗證，execute-time Core output 拒絕會移除已插入 output 並恢復已抽 ingredients。`×1/×8/×64/Max` 服從 terminal toggle，EMI Cursor/Inventory 不讀它。Crafting menu 現為 149 slots/95 data slots，output rail 只在 Storage/Craftable 顯示，Fuel forged click fail closed。GREEN 為 `compileJava`、`build`、SelfTest 222946/222946、GameTest 238/238、focused Python 3/3、full Python 98/98、`runData` no-new-drift 與 `runClient --dry-run`；這仍不是 fullscreen GUI verdict。

2026-07-14 shared-terminal Task 6 已依 RED→GREEN 完成：`MachineEnergyTable` 將九個 descriptors 分為五個 PROCESS、三個 INSTANT與一個 CONSUMABLE；process 維持 stack `N` 產生 `N/tick`，instant server-side max one且可取回，axe descriptor改用不保存物品的 transient input。`AxeEnergy` 只接受 axe abilities，finite value為 remaining durability × (vanilla Unbreaking level + 1) × count；Mending/其他附魔不加值，overflow/conflict/已有∞都在 consumption 前原樣拒絕。Core NBT 新增 finite long與 explicit infinite flag；Unbreakable canonicalize為∞且recipe不遞減，後續axe拒絕；legacy slot 8只在同規則 conversion成功後清除，失敗原物由 Axe Energy 格代理顯示並可取回；legacy instant overstack保留一個並把 extras 回收至 Core。Synthetic strip/scrape/wax-off discovery、presentation、Max、commit都改讀 exact reserve，resource metadata有 explicit infinity bit，blocked output rollback不扣值，per-key long-count mutation可一次處理 `Long.MAX_VALUE`。Crafting menu仍149 slots，data parity 95→100；Fuel heading改為 Stations & Axe Energy。Void lab保留legacy migration baseline，另固定提供plain、damage 100 Unbreaking II與Unbreakable axes，fullscreen checklist不再要求取回/重裝axe。初始 RED為66個預期missing-API compile errors、1個Fuel UI static failure與2個GUI-lab/checklist failures；review RED另鎖住 failed migration recovery、legacy instant overstack、explicit infinity與bulk commit。GREEN為`compileJava`、`build`、SelfTest 222949/222949、GameTest 251/251、full Python 99/99、`runData` no-new-drift與`runClient --dry-run`。這仍不是fullscreen GUI verdict。此段legacy migration行為已由2026-07-17 no-migration repository revision取代，只保留為歷史驗證紀錄。

2026-07-14 shared-terminal Task 7 已依 RED→GREEN 完成：SelfTest先以 missing `fuelTargetListButton`/`PopupList`/exact hitbox APIs compile errors形成 RED，三個 focused static tests固定 cycle-only selector、popup lifecycle/EMI與 whole-flow-cell hover；overlay review再用 click-through/covered-slot tooltip與舊Prism checklist failures形成 RED。Production保留 shared left-next/right-previous cycle，新增由 ordered server-approved targets產生的 representative-item/localized-name/selected-state popup、最多六列 bounded scroll、selected reveal、outside/Escape/page-leave focus cleanup、dynamic EMI exclusion與底層 slot input/tooltip suppression；machine/reserve tooltip只在實際18px station slot/16px representative icon上觸發。最終 `compileJava`、`build`、SelfTest 222959/222959、GameTest 251/251、full Python 102/102、`runData` no-new-drift與`runClient --dry-run`全綠。Prism fullscreen popup/selected-state verdict仍待整批最終gate。

2026-07-14 shared-terminal Task 8 已依 RED→GREEN 完成：focused static RED 精確抓到無 model 引用的 `block/import_bus.png`/`block/export_bus.png`、已停用的 `gui/grid.png`/`gui/crafting_grid.png`、其中一張 32×32 orphan，以及缺少新 family manifest。依 repo-local `minecraft-texture-replicate` workflow 使用 `retro-diffusion/rd-fast`、seed 71421，先從四張 chassis candidates 選 `shared_chassis_04`，再以 block strength 0.38 / item strength 0.68 產生 role candidates；候選與 selected contact sheets 均在 runtime replacement 前檢視。Final cleanup鎖定14色 palette與共用外框 pixels，明確畫出 Core rune、Storage item grid、Crafting 3×3 mark、T1–T6 六段 bars、藍色 inward Import、橘色 outward Export、Remote display，以及白色 fixed-canvas A/bar/grid/#/magnifier/@/arrows control atlas。`selection.json` 保存 repo-relative source/metadata、roles、hashes與 atlas index；raw predictions/prompts/previews/assembly script全留在 `art/texture-generation/20260714-terminal-family/`，jar只含16個 model-referenced 16×16 RGBA block/item PNG、`icons.png`與`terminal_controls.png`。Focused Python 2/2、full Python 103/103、`build`與jar resource inventory已綠；Task 9仍須重跑完整 GameTest/datagen並由使用者做 current fullscreen texture verdict。

2026-07-14 shared-terminal Task 9 automated release gate 已完成。Consistency RED 先由新 Python contract 證明現行 manual GUI log 段落仍鎖死歷史 `SelfTest 40608` 與 0.1.15；GREEN 改為任意 non-negative totals 但必須 `0 failed`，並要求每版 current-run artifact。其餘 active docs 的 installed-axe/variable-ledger/shared-terminal/Name-Quantity-Mod-ID/Player-Storage output 契約與程式一致；有日期與 replacement 指向的舊 counts 保留為歷史證據，未當成 current truth。Final 0.1.16 為 compileJava/build、SelfTest 222959/222959、GameTest 251/251、Python 104/104、runData no-new-drift、runClient dry-run、78 JSON/model/texture/jar checks 全綠。`deploy_prism_dev.py` 已將 0.1.15 備份至 `magic_storage_backups/20260714-143929/`，部署唯一 `magic_storage-0.1.16.jar`；build/deployed SHA-256 均為 `2ca800b1edbf643f278064d1d3392414fc2c48b529a1003b3e2ce7d6fbbd350a`。Automated gate 不是 GUI verdict；只剩用 current offline runner 到 READY 後交由使用者做 fullscreen checklist。

2026-07-14 shared-terminal Task 10 automated gate 已完成。RED 先精確證明 live Bottle Energy、缺少 lossless migration/overflow recovery、缺少 Import passive capability，以及 popup/no-recipe/station/output/i18n/texture contracts；GUI-lab與checklist另有獨立 RED。GREEN 退役 live pool，只在 Core NBT保留 raw `bottle_fuel`，以 plain Glass Bottle + persisted overflow escrow無損轉換並保留 retired 4-word zero wire；Import Bus 保留 directional front pull，同時在所有 sides/null side提供 stable insert-only capability。Review RED 再抓到 passive no-Core probe繞過節流、繁中 Brew Energy誤譯，以及飽和 bottle escrow 在 crafting transaction 內提前 refill 導致 rollback failure；active/passive lookup現共用10-tick negative cache，翻譯固定為「釀造能量」，escrow refill延後到outermost mutation batch結束，Auto equal-scarcity/amount tie-break coverage也已補回。Fuel popup只畫一次 foreground，recipe保留 selected identity顯示 neutral no-recipe、右下 dim station badge與Player Head/Core destination icon；cycle tooltip不再教手勢，runtime en/zh 74/74 keys exact parity。16×16 family使用71422–71424 revision seeds、x=7.5 symmetry、tier fill bands、Storage 2×2/Crafting 3×3與cyan-in/orange-out bus grammar。Final gate：compileJava/build、SelfTest 222956/222956、GameTest 262/262、Python 110/110、runData no-new-drift、runClient dry-run、78 JSON parse與diff check全綠。`deploy_prism_dev.py` 已 bump 至 0.1.17，將 0.1.16 備份到 `magic_storage_backups/20260714-172323/`，並原子部署唯一 `magic_storage-0.1.17.jar`；build/deployed SHA-256 均為 `3da6203ba16e3ad0a5b090955e5214dbf4fb33415c8b0bb06620fadf26caa93e`。此段只代表 automated baseline，後續 current-run visual verdict 見下一段；其中 Bottle Energy/escrow migration 行為已由 2026-07-17 no-migration repository revision 取代。

2026-07-14 0.1.17 current-run `crafting-fuel-page` 已在 `build/gui-runs/20260714-172548-crafting-fuel-page/` 到達 READY：唯一 Magic Storage jar 為 0.1.17，SelfTest 222956/222956，只有精確白名單的 offline profile-properties 401。使用者 fullscreen verdict **失敗，不可標記 GUI verified**；四張證據保存在該 artifact 的 `user-feedback/`。確認問題為 recipe empty prompt 少最後一個字母、`x1/x8/x64/Max` 像四顆分離的 vanilla buttons、Craft Output: Storage 被誤畫藍色狀態燈、`Stations & Axe Energy` 混合三種不同語意、Fuel page 兩列 machines + separate reserves/control 仍有不平衡空白，以及 Storage Unit tiers 主要只靠容量條區分。Active replacement 是 `docs/superpowers/specs/2026-07-14-connected-progression-fuel-guide-design.md` 與同名 plan；任何舊 READY/automated gate 都不能覆蓋這個目視失敗。

2026-07-14 0.1.18 replacement automated/deployment/handoff gate 已完成。Fusion mod的Gradle dev依賴先以static RED與真實`runData` RED證明不可放`AdditionalRuntimeClasspath`，現由client/data runs專用的隔離`fusionRuntime` source set載入；GameTest server generated classpath明確不含Fusion。Gradle 10 publishing URL space-assignment也先RED再改成`url = uri(...)`。Final為Python 146/146、compileJava/build、SelfTest 129163/129163、GameTest 269/269、runData載入Fusion且datagen無drift、runClient dry-run、13組connected sheets/metadata、雙語各22個guide JSON、jar無embedded Fusion/nested jar及diff check全綠。部署器bump至0.1.18，build/deployed SHA-256皆為`d4219e441668b0262cbb6247ec0f796d4cf3c5fa9738b1eb47173783c15764bc`，Prism mods只有唯一Magic Storage與SHA-512 `50604fa…365c`的Fusion。第一次current launch因primary monitor不可用而fail-closed並清掉本次process；activity wake後同一offline scenario在`build/gui-runs/20260714-211556-crafting-fuel-page/`到READY。Log載入Magic Storage 0.1.18、Fusion 1.2.12、SelfTest 129163/129163與Patchouli總計44 JSON（en_us/zh_tw各22）；唯一ERROR/Caused-by是精確白名單的offline profile-properties 401，不是Microsoft/Xbox登入。Checklist已改成只禁止non-whitelisted errors，不再與這個已知401自相矛盾。目前只保留這一個Minecraft給使用者先切fullscreen再目視；尚無visual verdict，不可標記GUI verified。

2026-07-14 0.1.19 screenshot correction automated/deployment/handoff gate 已完成。使用者0.1.18截圖指出空recipe仍留下無語意的大型ledger框、Fuel descriptors仍是物品/數字橫向對照、type capacity壓到Inventory label。Focused Python先有4個預期failures，SelfTest契約再以18個missing geometry compile errors形成RED；GREEN改為每格上方18px slot/16px icon、下方9px置中且cell-local縮放amount，Instant Stations右下保留descriptor paging永不侵入的type-capacity status，empty recipe把diagram+ledger畫成一個neutral surface。Final為Python 150/150、build、SelfTest 213726/213726、GameTest 269/269、Fusion-backed runData且resource hash無new drift。部署器自動bump至0.1.19，build/deployed SHA-256同為`808fd6e8b36da98f2940cedf9c89aed3277fd64aee2186b3bbe444840b934082`，Prism mods只有唯一Magic Storage 0.1.19與釘選Fusion。舊0.1.18 client以exact PID正常關閉，未開/未留下Terminal.app；新offline session `build/gui-runs/20260714-224250-crafting-fuel-page/`已以wrapper/error-console disabled、automatic Minecraft F11到`MS_GUI_TEST_READY`，log為SelfTest 213726/213726且只有精確白名單的profile-properties 401。使用者fullscreen verdict **再次失敗，不可標記GUI verified**：Fuel Target仍壓在Consumables heading、三列缺乏清楚的橫向分組、Instant Stations侵入Inventory label，而有效recipe仍把單一材料列下面的整片高度畫成大型白色空區。0.1.19 READY只保留為已否決的視覺證據。

2026-07-14 post-0.1.19 dark-workspace replacement 已完成RED→GREEN、完整automated gate與0.1.20部署。新Python contract先在缺少`drawTerminalSurface`時精確失敗；dedicated SelfTest再得到214097 passed / 8301 failed，失敗集中在缺少Fuel左側label reservation、selector過寬、recipe body無高度上限及單列ledger未置中。Production改用`20260714-terminal-family/selection.json`既有深色palette：Fuel/recipe workspace是blue-charcoal surface + slate semantic cards，內部禁止大型`0xFFE2E2E2`白底與純白bevel。Fuel改成獨立compact target bar，再接三個左側bounded label strip／右側content row；保留上物品/slot、下數字，空station以dim代表物提示用途，type-capacity cell只畫compact `stored/max`且完整句子移到tooltip，最後一列與Inventory間固定保留13px label band。Recipe diagram+ledger形成最高108px、ledger最高36px的compact centered card，單一resource row在ledger內置中，footer/導航/`x1/x8/x64/Max`使用同一深色control grammar。Final為compileJava、build、Python 155/155、SelfTest 210828/210828、GameTest 269/269、Fusion-backed runData no-new-drift與diff check。部署器bump至0.1.20，將0.1.19備份至`magic_storage_backups/20260714-232112/`；build/deployed SHA-256皆為`3d54527d8d04e930334d1ccddfb545b11f91183ae09074471a978f2683beb524`，Prism只含唯一0.1.20及釘選Fusion。第一次current `crafting-fuel-page` launch在READY前因macOS未暴露primary monitor而fail closed且已清理；`caffeinate -u`及Computer Use仍得到`cgWindowNotFound`，必須先實際喚醒並解鎖螢幕後原樣重跑，尚不可標記GUI verified。

2026-07-12 現場追查:21:04 的 `build/gui-runs/20260711-210426-patchouli-guide/session.json` 是舊 schema，仍含 `computer_use_wrapper`；該次雖以 `-o MagicStorageBot` offline 啟動，但 `ShowConsoleOnError=true` 讓 authlib 401 console 跳出，client 在 21:10:58 記錄 `Stopping!` 後退出。新 session 必須在 `session.json` 看到 `launch_profile.computer_use_wrapper_disabled=true`、`launch_profile.error_console_disabled=true`，且不可再有 top-level `computer_use_wrapper`;否則是舊 artifact，不能當作目前流程已驗證。

以下 Computer Use wrapper 資訊只保留作診斷/相容性背景，不是目前 visual gate 的預設操作者：

當需要由 Computer Use 讀取/操作 Minecraft 視窗時,不要直接 target Prism 產生的 `java` 子程序:Prism-launched LWJGL 視窗沒有正常 `.app` bundle identity,`get_app_state("java")` / `get_app_state("Minecraft...")` 會失敗。已驗證可行做法是用 wrapper 讓 Prism 透過真正 macOS app 啟動 Java:

- app bundle:`/tmp/MagicStorageMinecraftCU.app`,bundle id:`run.hapi.magicstorage.minecraftcu`。Computer Use 用 bundle id: `get_app_state("run.hapi.magicstorage.minecraftcu")`。
- 只有刻意改回 Computer Use 診斷時，Prism dev `instance.cfg` 才需要 `OverrideCommands=true` 與 `WrapperCommand=/tmp/magic_storage_minecraft_cu_wrapper.sh`;用 `python3 scripts/setup_prism_computer_use_wrapper.py` 建立。正常 `run_prism_gui_session.py` 會再次清空 `WrapperCommand`，確保交給使用者的是 native Minecraft 視窗。
- wrapper 必須把 Prism `stdin` 透過 FIFO 轉接進 `.app` 內的 Java;Prism 的 `NewLaunch.jar` 會從 stdin 讀 launch script,單純 `open .app --args java ...` 會因 EOF 變成 `Launch aborted by the launcher`。
- `run_prism_gui_session.py` 直接執行 `"/Applications/Prism Launcher.app/Contents/MacOS/prismlauncher" -l dev -w MagicStorageGuiTest -o MagicStorageBot`；啟動前硬性要求Prism 11.0.3+與已完成初始化、保持開啟的normal-root Prism process。這只用於單人GUI smoke/目視，使用normal Prism root的既有owning account但要求本次launcher片段沒有真正Microsoft/Xbox auth step；不要把它當成線上登入或skin/profile驗證。
- 目前 runtime wrapper/app 仍生成在本機 `/tmp`,但已由 repo 腳本 idempotently 重建;若 `/tmp` 被清掉,重跑 `python3 scripts/setup_prism_computer_use_wrapper.py`。
- Computer Use 能讀截圖與送鍵盤;LWJGL canvas 可能讓 `click` 回 `noWindowsAvailable`,選單優先用 `Tab`/`Return`。遊戲內要觸發右鍵時,最穩的是啟動前暫時把 `options.txt` 的 `key_key.use` 改成鍵盤(例:`key.keyboard.u`)並把 `pauseOnLostFocus=false`,用完立刻還原;滑鼠視角可用 CGEvent delta 輔助,不要用 Computer Use drag 對準方塊(會等同左鍵破壞方塊)。
- LWJGL 文字輸入目前不可靠:Computer Use `type_text` / paste 對 Minecraft chat、creative search、terminal search 不穩。能用點擊/滾輪完成就避免輸入;Patchouli guide 可從 creative search tab 捲到底部拿 `Magic Storage Guide` 再用 `u` 開。若一定要送 chat 指令,先把 macOS input source 切到 `ABC`;注音輸入法會讓 `/tp`/`/setblock` 之類指令輸入成錯字。
- 2026-07-07 Prism dev fullscreen 實測:Storage Terminal 與 Crafting Terminal 都能開啟且版面未與 EMI 重疊。
- 2026-07-08 Prism dev 已部署並實機驗證 `magic_storage-0.1.3.jar`(0.1.3 build + GameTest 已驗證,mods 內僅此一個 Magic Storage jar)。`latest.log` 看到 `SelfTest: 104 passed` 與 `BookContentResourceListenerLoader preloaded 17 jsons`;Patchouli content 路徑問題已修。Computer Use 在 native macOS fullscreen 下已可讀畫面並操作 Storage Terminal / Crafting Terminal:左側 view buttons 可點、>64 數量顯示為 128/192/8192 等實際網路數量。當 macOS 處於 lock screen 時,Computer Use 會 `cgWindowNotFound` 且全螢幕截圖只會看到鎖定畫面,必須先解鎖再做 GUI 目視/點擊驗證。
- **Historical baseline:** 2026-07-08 的 Computer Use wrapper/Stage Manager、2026-07-14 的monitor-backed F11與Prism 11.0.2離線refresh問題都不是現行流程。2026-07-22短暫使用的Offline-only isolated `-d` root也已否決，因Prism會在沒有owning account時進Demo。現行runner要求Prism 11.0.3+與已warm的normal-root process，以明確app binary path和`-o MagicStorageBot`透過現有process啟動既有dev instance，並只以本次log cursors判定READY/auth。
- **Current macOS borderless F11 contract:** `MacOsWindowMixin` 將 macOS F11 改為無邊框 Cocoa 視窗、隱藏 Dock/menu bar，**不會把 GLFW 視窗 attach 到 monitor，也不會選擇或切換 desktop display mode**。`prepare_prism_gui_world.py` 會刪除 `fullscreenResolution`，並在 manifest 記錄 desktop mode；`run_prism_gui_session.py` 在 READY 後再次驗證點數、像素、refresh 與 depth 都未改變，否則 fail closed、不交接 GUI。完整操作與排查以 [`macOS Minecraft F11 全螢幕與關閉指南`](macos-fullscreen-guide.md) 為準；仍禁止 macOS 原生 fullscreen（綠色按鈕、Control-Command-F）與兩種 fullscreen 疊加。
- **Current close contract:** 歷史上，直接在 F11 fullscreen 按 Command-Q 曾在 `Stopping!` 後讓 Java 主視窗變成 0×0，並卡在 `Minecraft.destroy → disconnect → updateScreenAndTick → runTick → Window.updateDisplay → glfwSwapBuffers`，不是 mod/server deadlock。因此現行唯一允許順序是 **F11 離開 Minecraft fullscreen → 等正常有標題列的 windowed 視窗出現 → Command-Q**。不得在 F11 fullscreen 直接 Command-Q；watchdog 只是在精確測試 Java process 在 `Stopping!` 後五秒仍存活時的最後防線，不能替代正常關閉證據。
- runner 現在把兩層 recovery 都自動化：每次 launch 前按完整 dev instance path、Prism markers 與 process tree 清掉同一測試 instance 的 stale Java/Prism process，其他 Prism instance、Terminal.app 與無關 Java 不動；visual READY 後再啟動 detached shutdown watchdog，綁定這次 Java 的 exact PID + 完整 command fingerprint，並只讀 handoff 後的 log cursor。正常退出會寫 `shutdown.json`；若看到本次 `Stopping!` 後 exact process 仍存活五秒，就只對該 Java PID 送 SIGTERM，既有 termination helper 再於必要時升級 SIGKILL，結果記為 `forced_after_glfw_shutdown_stall`。watchdog 設定與輸出在同一 `build/gui-runs/<timestamp>-<scenario>/shutdown-watchdog.json`、`shutdown-watchdog.log`、`shutdown.json`；固定測試世界本來就每次 transactionally 重建，因此這個 fail-safe 不會被誤當成一般玩家世界的保存策略。
- 2026-07-11 已實測 `python3 scripts/run_prism_gui_session.py --scenario boot-smoke`:runner 用 `-o MagicStorageBot` 開啟 Prism dev `MagicStorageGuiTest`,只讀本次 log offset,看到 `SelfTest: 104 passed`、Patchouli `preloaded 17 jsons`、`MS_GUI_TEST_READY`。Offline client 仍會記一段已知 `Failed to fetch user properties` / 401 authlib noise;runner 只白名單這段,其他 current-run `advanced_container_set_data` / `ERROR` / `FATAL` / `Caused by` 仍會失敗。這是非目視 smoke;沒有取代下方全螢幕 GUI gate。

固定 GUI session 流程（自動進入 Minecraft F11 fullscreen，再強制通過「全螢幕 gate」）：

```bash
# 非視覺 smoke:只驗本次 client boot / SelfTest / ready log,不需要目視
python3 scripts/run_prism_gui_session.py --scenario boot-smoke

# 終端/Patchouli/可見 GUI 變更:runner 等 READY 後停止,交給使用者手動目視
python3 scripts/run_prism_gui_session.py --scenario terminal-left-rail

# Import/Export Bus 設定畫面、方向/無方向模型與共享控制語法
python3 scripts/run_prism_gui_session.py --scenario bus-configuration

# Crafting Terminal Fuel 頁:只產生這次功能需要的 fullscreen checklist
python3 scripts/run_prism_gui_session.py --scenario crafting-fuel-page
```

`run_prism_gui_session.py` 在啟動前先fail-closed驗證Prism Launcher至少11.0.3、Prism mods內恰好一個目前build的Magic Storage jar，以及恰好一個檔名、版本與SHA-512都符合pin的Fusion 1.2.12 jar；版本太舊、缺少、重複、同名但內容不同都在清理process或改世界前停止。jar問題要求先重跑`python3 scripts/deploy_prism_dev.py`。通過後先精確清理同一dev test instance的stale process，才清空wrapper、關閉instance error-console pop-up、transactionally重建true-void lab、以明確`/Applications/Prism Launcher.app/Contents/MacOS/prismlauncher`啟動`-w MagicStorageGuiTest -o MagicStorageBot`，再從本次log cursor polling `SelfTest:` + `MS_GUI_TEST_READY`；cursor同時記byte offset + device/inode，`latest.log`被rotate/replaced時會從新檔開頭讀，不會漏掉current-run error。runner把`manifest.json`、`session.json`、`log-excerpt.log`、`checklist.md`寫到`build/gui-runs/<timestamp>-<scenario>/`。visual scenario READY後JSON會包含`manual_handoff_message`並啟動exact-process shutdown watchdog；不要再接自動滑鼠/鍵盤步驟。預設`--timeout 300`，保留給冷啟動/資源載入/世界載入；不要改用固定短sleep。若只要預產artifacts不啟動Prism，可加`--no-launch`。

`prepare_prism_gui_world.py`(通常由 session runner 呼叫)只會覆蓋帶有 `.magic_storage_gui_test_world` marker 的 `MagicStorageGuiTest`;若同名世界不是腳本生成、來源等於目標、或來源/目標世界仍被 Minecraft 開啟會直接失敗。複製先完成在 sibling staging，成功後才把舊 generated target 換成 backup 並原子交換；copy/NBT/datapack 任一步失敗都不會先刪掉可用測試世界。`New World` **只提供目前 1.21.1 相容的 `level.dat` metadata**：腳本明確移除複製來的 `region`、`entities`、`poi`、`data`、`datapacks`、三個 dimension 路徑、server/player runtime state、lock/old level/icon，並刪除單人世界另外嵌在 `level.dat/Data/Player` 的 position/dimension/inventory；再把 overworld generator fail-closed 重寫成 `minecraft:flat` + `minecraft:the_void` + 單層 air、`features=false`、`lakes=false`、空 `structure_overrides`。因此新世界是真正無地形/無結構的 void，不是普通世界高空清場；來源世界完全不被修改。最後才安裝 datapack `magic_storage_gui_test`，移除可能殘留的 `fullscreenResolution`，並 patch Prism `options.txt`(`fullscreen:true`,`pauseOnLostFocus:false`,`key_key.use:key.keyboard.u`,`guiScale:4`,`overrideWidth:1280`,`overrideHeight:720`)；`overrideWidth/Height` 只保留給 windowed fallback。macOS 上 `fullscreen:true` 會進入 Magic Storage 的 borderless Minecraft F11，而不是 monitor-backed GLFW fullscreen 或 macOS native fullscreen：它不切 desktop resolution，runner 會在 READY 後用 manifest 記錄的 desktop mode 再驗一次。datapack 只用 `load`/`tick`/hotbar-triggered functions,不使用 command block。

datapack `load`先建立scoreboard並重建Y=80 true-void lab；tick等待3個game ticks後固定player kit、prime hotbar latch、加ready tag並`say MS_GUI_TEST_READY`，沒有wall-clock sleep。active network仍是Core +兩個terminal + T6→T1六單元spine（總容量785 types）+兩側bus/barrel；`z=-9`是斷開的isolated gallery，`z=-11,x=-5..4`是無Core的contiguous connected gallery。reset只用plain `setblock`放fresh Core，不得注入inline inventory/energy/machine NBT；server自行建立repository record，baseline inventory、timed/instant stations、Axe Energy與Fuel reserves全空。player kit提供成對Crafting Table/Stonecutter/Smithing Table、process machines/fuels、plain Iron Axe、damage 100 Unbreaking II Iron Axe與Unbreakable Iron Axe，讓使用者實際驗max-one與finite/∞轉換。hotbar `1`–`8`導航、`9`做atomic reset；玩家接手後按數字鍵，datapack在下一tick `/tp facing`對準目標，再按`u`開GUI。

| Hotbar key | Target | Block | Function | Notes |
|------------|--------|-------|----------|-------|
| `1` | Storage Terminal | `(-1,80,0)` | `/function magic_storage_gui_test:view_storage_terminal` | 十字準心對準後按 `u` 開儲存終端 |
| `2` | Crafting Terminal | `(1,80,0)` | `/function magic_storage_gui_test:view_crafting_terminal` | 十字準心對準後按 `u` 開合成終端 |
| `3` | Storage Core | `(0,80,0)` | `/function magic_storage_gui_test:view_storage_core` | 看 Core tooltip/破壞重放測試 |
| `4` | T6→T1 spine | `(0,80,-1..-6)` | `/function magic_storage_gui_test:view_storage_unit_t6` | 六 tier 全預接 Core，785 type capacity |
| `5` | Import Bus | `(-1,80,-1)` | `/function magic_storage_gui_test:view_import_bus` | 朝 west，外側 `(-2,80,-1)` 有 barrel |
| `6` | Export Bus | `(1,80,-1)` | `/function magic_storage_gui_test:view_export_bus` | 朝 east，外側 `(2,80,-1)` 有 barrel |
| `7` | Texture/Wrench gallery | isolated `z=-9`; connected `z=-11` | `/function magic_storage_gui_test:view_texture_gallery` | 手持 Wrench；同時比較 ordinary 16×16角色圖、Fusion接縫、bus方向front、旋轉與安全拆除；按`9`立即還原 |
| `8` | Lab overview/home | n/a | `/function magic_storage_gui_test:home` | 回到俯視完整平台的固定視角 |
| `9` | Atomic reset | n/a | `/function magic_storage_gui_test:reset_from_hotbar` | 呼叫同一 `setup`；3 tick 後重發 deterministic kit/READY，不直接遞迴 player-ready |

GUI 測試項目仍由當次變更動態決定;先由 `run_prism_gui_session.py --scenario <name>` 產生 checklist。scenario 顯示 `Manual GUI required: yes` 時，runner 等本次 `MS_GUI_TEST_READY` 後立即交給使用者；所有 GUI 測試都必須先通過全螢幕 gate。Minecraft 已自動進入 F11 fullscreen，使用者只確認畫面沒有偏移/裁切。任何 `u`、hotbar、點擊、滾輪、截圖前,都必須先通過全螢幕 gate；之後只執行當次 checklist 必要的項目。`/function` 保留給人類手動輸入。runner 的等待只用 log condition polling，不用固定 sleep。

runner 顯示 handoff 後，使用者在已自動進入 borderless Minecraft F11 的視窗操作；runner 已先驗 desktop display mode 未變。因為 `--world` 會跳過主選單/世界列表，直接確認以下 gate，未通過前不可開始 GUI 測項，也不可用 macOS 綠色按鈕或 Control-Command-F「再開一層」fullscreen：

- Minecraft 內容沒有被 Stage Manager 側欄/桌面裁切。
- 遊戲畫面左下角版本文字與底部 hotbar/GUI 坐標完整可見。
- 使用者能看到完整視窗邊界與正常十字準心/GUI 對齊。

只有以上通過才算「好全螢幕」與全螢幕 gate passed。所有 GUI 測項都在這個 Minecraft F11 fullscreen 狀態下進行;然後依當次需求按 hotbar `1`–`9` 對準/巡覽/重置、按 `u` 開 GUI、檢查畫面與 log。runner 每次都重新寫入同一 fullscreen contract，不依賴上次退出時保存的選項。結束時一定先按一次 F11，等普通有標題列的 windowed 視窗完整出現後才按 Command-Q；不得直接從 F11 fullscreen Command-Q。若仍碰到 GLFW shutdown stall，讓 runner watchdog 處理並檢查該 run 的 `shutdown.json`，不要手動廣泛 `pkill java`。完整處理見 [`docs/macos-fullscreen-guide.md`](macos-fullscreen-guide.md)。

```bash
rg -n 'MS_GUI_TEST_READY|Magic Storage [0-9.]+|SelfTest: [0-9]+ passed, 0 failed, [0-9]+ total|BookContentResourceListenerLoader preloaded|advanced_container_set_data|ERROR|FATAL|Caused by' \
  "$HOME/Library/Application Support/PrismLauncher/instances/dev/minecraft/logs/latest.log"
```

期望:看到 `MS_GUI_TEST_READY`、目前部署版本、一行符合 `SelfTest: [0-9]+ passed, 0 failed, [0-9]+ total`、目前完整雙語指南對應的 Patchouli `preloaded 44 jsons`（en_us/zh_tw各22）,且除了 offline `Failed to fetch user properties` / 401 authlib noise 外沒有 `advanced_container_set_data` / `ERROR` / `FATAL` / `Caused by`。2026-07-09 已實測:固定 world 載入後通過全螢幕 gate,hotbar `1` 對準 Storage Terminal、hotbar `2` 對準 Crafting Terminal,按 `u` 都可在 fullscreen 下開 GUI。所有舊 handoff 只屬歷史證據；每次改版都必須重新產生 current-run artifact 並由使用者完成 fullscreen 動態 checklist，不得沿用舊版本 verdict。

## Reference Source — Refined Storage 2

實作儲存機制、網路邏輯、grid UI、物品/流體處理卡關時,**先參考 Refined Storage 2 原始碼**。

| Topic | Where to look |
|-------|--------------|
| 儲存網路架構 | `refinedstorage-common/src/main/java/.../network/` |
| Grid(物品瀏覽 UI) | `refinedstorage-common/src/main/java/.../grid/` |
| Storage provider / disk API | `refinedstorage2-api/src/main/java/.../storage/` |
| 物品/流體資源處理 | `refinedstorage2-api/src/main/java/.../resource/` |
| 自動合成 | `refinedstorage-common/src/main/java/.../autocrafting/` |
| NeoForge 平台橋接 | `refinedstorage2-platform-neoforge/` |

Source: https://github.com/refinedmods/refinedstorage2

卡關工作流:1) 在 RS2 repo 搜你要的介面/類別 → 2) 讀實作理解模式 → 3) 改寫(**勿整段照抄**,授權不同)→ 4) 仍不確定就問使用者。

## Gotchas

- **網路邏輯一律伺服器端**;客戶端靠封包同步,絕不在客戶端保存儲存狀態。
- **Crafting Terminal Fuel categories 契約**:`MachineEnergyTable` 的九個 descriptors 有明確 category。Furnace/Blast Furnace/Smoker/Campfire/Brewing Stand 是可堆疊 process machines，stack `N` 每 tick 只為對應 pool 加 `N`；Crafting Table/Stonecutter/Smithing Table 是 max-one instant stations，只解鎖 recipe 且可取回；axe是consumable input，成功接受後原子轉成finite/∞值，不留persistent item，拒絕時原stack保持不變。有限 Axe Energy = remaining durability × (vanilla Unbreaking level + 1) × accepted stack count；Mending/其他 enchantment 不加值，checked overflow 必須在吃物品前拒絕。Unbreakable 設 explicit persistent infinite flag、finite canonicalize 為 0、recipe 永不遞減，之後的 axe 原樣拒絕。每個installable descriptor只接受目前max count，remainder留在玩家原stack；本次repository格式不讀舊slot或執行migration。空 process slot 不產能，移除停止但保留累計量；conflict 暫停產能並拒絕所有安裝/轉換，但允許取回既有 station。燃料只在 Fuel 頁轉換；Storage 頁把機器與燃料當一般物品儲存。UI 先畫獨立compact Fuel Target bar，再依 descriptors 的 category 產生三個獨立、全寬category panels：**Consumables** content包含input/Fuel/Brew/Axe Energy，**Timed Stations** 包含五台 process machines與對應 reserve，**Instant Stations** 包含三台 unlock-only station且不畫假能量。三個panels從標題下方開始，平均分配到Inventory label band之前的完整垂直空間；不得回到 `Stations & Axe Energy` 混合兩列、把target塞進Consumables heading，或加回右下`fuelControlPanel`。每個panel左側有固定label strip，右側是adaptive multi-row paged flow；每個descriptor cell固定上方18px slot/16px icon、下方9px amount bounds，數字置中並只在自己的cell內縮放。空machine slot須在真slot後方畫dim代表物但不可冒充installed stack。Type capacity改在player inventory正右方的獨立info panel顯示完整localized `stored / max types`，panel上下與inventory對齊；Instant Stations使用完整category width。每頁容量由可用寬高決定，稀疏頁平均使用content bounds，overflow由該panel的頁碼與wheel paging處理；slot/tooltip只認實際slot/icon。SelfTest用每類64個descriptors鎖定多頁可達性、無重疊與小視窗geometry。Fuel target selector與bounded list popup都使用vanilla widget/container grammar；左/右/滾輪維持 next/previous，**中鍵明確 reset Auto**，server 只接受 Auto/exact target IDs。Popup仍須處理 bounded scroll、selected reveal、outside/Escape/page-leave close、focus cleanup、EMI exclusion與click-through suppression。**第三方 descriptor API 已完成：addon 透過公開 NeoForge registry 在正常 mod registration lifecycle 註冊；server凍結/cached ordered snapshot、opening payload、clientbound consumable state、固定256 menu parity與stable-ID world persistence均為現行契約。這不是registry freeze後的hot runtime registration。**
- **Terminal cycle/status/reset 契約**:所有 cyclic value selector 都是 left-next/right-previous/wheel-down-next/wheel-up-previous/middle-reset；reset defaults 為 Name、Ascending、normal search、Item resource view、Auto Fuel Target、Player Craft Output及既有 player-source default，且必須送 explicit server button action。只有 true boolean on/off control可畫彩色狀態燈；Craft Output、sort/search/resource view/Fuel Target等 value selectors不可亮綠/藍 marker。Page tabs可用 neutral selected treatment，但不能冒充 on/off light。Tooltip只顯示控制名稱與目前值，操作手勢寫在指南書。
- **Wrench/connected texture 契約**:Magic Storage 自帶 early Wrench 並加入 `c:tools/wrench`，同時接受其他 mod 在該 tag 的工具。普通主手右鍵只旋轉有方向的 network block；Shift+主手右鍵以正常 loot path拆除任何 network block，含資料Core必須先建立指向同一repository record的recovery mapping，掉落token不得攜帶payload；玩家背包放不下的exact remainder必須掉在世界而不是void。Connected casing在安裝可選Fusion `1.2.12-neoforge-mc1.21.1`的client使用`pieced` layout；未安裝Fusion時主資源的vanilla model必須仍可完整遊玩。client僅在偵測到Fusion後才註冊並強制啟用內建`fusion_connected_casing` overlay；dedicated server不依賴，Prism deploy下載官方Modrinth jar並驗固定SHA-512 `50604fa…365c`，禁止bundle。Gradle dev環境只讓client/data runs選用隔離的`fusionRuntime` source set；server/GameTest維持main source set。**Fusion是mod，不能放進`clientAdditionalRuntimeClasspath`/`dataAdditionalRuntimeClasspath`**：NeoForge 1.21.1只會把該legacy path當一般library，依賴檢查仍會報Fusion未安裝；必須讓Fusion進對應run的標準runtime classpath，`runData`實際成功載入才算驗證。**不要照目前Fusion 1.3 wiki/examples寫schema**：釘選1.2.12的`match_block`只接受單一`block`字串；跨12種network blocks必須寫成12個predicate的array（OR），沒有`blocks`欄位，也沒有`true`/`false` predicates。Bus只為top/side指定連接predicate，ordinary front不需要假`false`。Item models維持普通16×16，bus正面方向語意不能被連接邊框吃掉。
- **Progression/guide 契約**:Starter Core + Storage Terminal + 2x T1 總共只用1 Diamond與一般 overworld black concrete powder/obsidian/copper/redstone/glass/wood，T1不需要amethyst；functional midgame terminal/wrench/buses/remote/T1–T3 不要求 Netherite、Ancient City、Trial Chamber或End completion。T4–T6是選擇性 prestige/capacity upgrades，禁止回到 Diamond Blocks/Netherite Ingots wall；exact matrix見 current connected-progression spec。Patchouli 必須有集中式雙語 Recipe Catalog，en_us/zh_tw category/entry/page/recipe parity，詳細但只講玩家概念，禁止 BFS/packet/hidden slot/wire parity/test totals等實作詞。
- **Recipe ledger 自適應**：正常寬度固定最多四欄，1–4 種在第一列、5–8 種進第二列、9–12 種進第三列並由上緣開始。若 recipe ledger 寬度不足每格 48px，欄數依可用寬度降為 3/2/1；不可為維持四欄而讓材料與數字互相覆蓋。
- **Patchouli book ownership**：book definition 的 `custom_book_item` 固定為 `magic_storage:guide_book`；該 item 由 Magic Storage registry/creative tab 管理，右鍵透過 Patchouli API 開啟 `magic_storage:guide`。指南配方是 Book + Black Dye + Copper Ingot，Patchouli 是 BOTH-side required dependency；不可再把「書內有 recipe pages」誤當成「指南書本身可合成」。
- **ContainerData wire 只有 16 bits/value**:`ClientboundContainerSetDataPacket` 在 1.21.1 用 `writeShort/readShort`;同步完整 `int`/`long` 都必須拆成 unsigned 16-bit words，client `set` 時先 `value & 0xFFFF` 再重組。直接把 server `DataSlot.get()` 的 int 塞進 client test 會產生假綠；base terminal 固定13（index 11是type-capacity explicit-unlimited bit，index 12是resource view），crafting/fuel固定89（8個recipe/page/destination values + 7個live energy `long` + 1個retired zero-filled parity `long` + 9個ingredient available `long` + process/Fuel required各一個`long` + Axe Energy `long` + infinite flag），總parity為102。Ingredient required以hidden representative的`TerminalDisplayStack` long metadata同步，stack count固定1；client必須先讀metadata再strip代表物。GameTest 必須用真實 packet round-trip 守住大於 32767 的值，並驗type capacity、resource view與Axe Energy各自的state。
- **Bus typed capability**:`RegisterCapabilitiesEvent`為Core/Import/Export註冊generic resource、fluid、FE與optional chemical，另保留Import insert-only及directionless Export extract-only的一格item handler。Directional active front會同時嘗試item、generic addon與全部native typed endpoints；generic存在不可遮蔽native。Import generic/native只insert，Export只extract，generic路徑拒絕Item kind。Schema-2 exact `resource` rule只匹配同typed key；exact/item/tag/mod規則只理解item，不得把代表物解釋成fluid/chemical filter。Stable wrappers每次call重驗current BE、escrow、mode/side/unsided、automation/filter、loaded Core/path與conflict；設定surface變更後invalidate capability，no-Core時wrapper保持stable但inert。
- 實作進度以**程式碼為準**:目前119個main Java檔；最新驗證數以`docs/plan.md`為準。設計細節見`PLAN.md`。
- 建置前需設 `JAVA_HOME`(JDK 21,見根 repo `docs/notes.md`)。
- **SelfTest 在 dedicated server 也會跑**(mod 建構時呼叫):絕不可從 SelfTest 參照 client-only 類別(如 `*Screen` extends `AbstractContainerScreen`),否則 server 端 RuntimeDistCleaner 會擋下、整個 mod 載入失敗。純邏輯放 dist-中性類別(如 enum:`SearchMode.apply`)再測。
- **Runtime furnace fuel 不能在 mod-constructor SelfTest 判定**:`ItemStack#getBurnTime(null)` 依 NeoForge furnace-fuel data map；SelfTest 在 data map 可用前就跑，coal/log 會暫時回 0。SelfTest 只測 explicit Brew mapping 與 pure tie-break；oak log/coal/modded runtime Fuel acceptance/value/overflow 必須用 server 已完成資料載入後的 GameTest 驗證。不可為了讓 early SelfTest 通過而加回 hardcoded Fuel fallback。
- **Client-only mod-bus listener 註冊**:`RegisterMenuScreensEvent` 這類 client-only mod bus event 由 `MagicStorage` 建構子在 `FMLEnvironment.dist == Dist.CLIENT` guard 內呼叫 `ClientSetup.register(modEventBus)`;不要用 NeoForge 1.21.1 已 deprecated-for-removal 的 `@EventBusSubscriber(bus = EventBusSubscriber.Bus.MOD)`。Dedicated server 必須仍能載入並跑 SelfTest/GameTest。
- **`Slot.x`/`y` 是 `final`**:GUI 動態調整版面時不能改 slot 座標,需在 Screen(client)端 `menu.slots.set(i, new Slot(...))` 重建 slot(座標只供 client 渲染/命中,不上傳)。重建必須保留每個 menu index 的原始 semantic delegate；不可在 repeated `init()` 中委派到上一次 wrapper，否則 resize 次數越多 wrapper chain 越深。見 active adaptive plan。
- **終端機背景/資產契約**:`StorageTerminalScreen.drawPanels`程式繪製終端背景，固定6列的`grid.png`/`crafting_grid.png`不可加回。Player inventory、Fuel與recipe semantic workspace必須共用vanilla light container grammar：raised panel、inset well、slot frame、vanilla widget sprites與dark-gray text；`20260714-terminal-family/selection.json`只屬於world block/item textures，不得再套入GUI。Runtime item/base textures維持ordinary 16×16 RGBA；world block casing預設為vanilla 16×16 model；只有偵測到可選Fusion 1.2.12時才由內建overlay以`pieced` 80×16 five-tile sheets連接，item model不可引用connecting sheet。T1–T6必須在舊meter區域外呈現monotonic ornament progression；Creative Storage Unit使用同chassis/palette的cyan-amethyst infinity cell，並在全部12種network block的Fusion predicates中互連。全部方塊以x=7.5做水平鏡射並以2×2偶數中心校正。Import/Export bus的front方向grammar不能被connected border覆蓋。所有生成metadata/raw/previews/scripts/contact sheets只能留`art/texture-generation/`；runtime tree不得有orphan/sidecar或credential。Static model/palette/symmetry/ornament/connection tests不能取代fullscreen visual gate。
- **終端 view 控制在左側 rail**:`TerminalProfile.STORAGE` 是 reduced profile，`TerminalProfile.CRAFTING` 組合 pages/recipe/Fuel/player-source capabilities；所有 screen 只可呼叫唯一 `TerminalLayout.forProfile(...)`，immutable `Geometry` 同時驅動 drawing/widgets/slot reconstruction/hitboxes/tooltips/scroll/EMI exclusion。`StorageTerminalScreen` 是 shared shell 並且是 common sort/order/search/resource-view controls 的唯一建立者；Crafting 只從 profile hook 加 page/source controls，不可 `super.init()` 後 hide-and-replace。Resource view只在Storage view出現並由server data slot同步，固定循環Item→Fluid→Energy→Gas/Chemical→Other；Other只含目前已註冊且不屬於四個built-in groups的addon kinds，missing provider仍隱藏。Craftable/Fuel forged resource-view button必須fail closed。所有 rail hitbox 固定 18×18、icon canvas 固定 16×16；page/source 使用語意 item，抽象 icons 使用 `terminal_controls.png` atlas，不可回退 Unicode 或 runtime `graphics.fill` glyph。Crafting 第一組固定 Storage/Craftable/Fuel tabs，`RAIL_GROUP_GAP` 分隔 page 與 view groups，Fuel rail 到三頁籤即結束。共用 cycle 契約為 left-next/right-previous/wheel-down-next/wheel-up-previous；player-facing tooltip 只列 control/current value，不列操作提示或所有選項。rail 寬度參與 narrow/side-by-side 判定與 rail+frame 置中；Fuel/recipe regions 仍全取同一 geometry。screen constructor 保存 `List.copyOf(menu.slots)` original semantic delegates，任何 repeated `init()` 都從原始 slot 重建。
- **終端數量文字不可侵入相鄰 slot**:`StorageTerminalScreen.renderSlotContents` 對 display slots 不走 vanilla 無界 decimal count；物品本體以 count 1 render，真實 synced count 經 `TerminalAmountFormatter` 轉不高估的 compact unit。`init()` 以所有允許 compact shapes 的最寬字串與 16px slot bound 算出單一 `networkAmountScale`，整個 screen 的每個正數都用同一 scale 右下對齊；不可依個別值切正常/0.5，也不可讓 shadow 或座標超出自己的 16px cell。零不畫字，exact long 留在 tooltip；玩家背包仍走 vanilla render，Fuel type capacity 沿用 base menu server sync。
- **終端顯示量不能塞進 `ItemStack#getCount`**:`StorageCoreBlockEntity.getDisplayStacks` 與 Craftable synthetic outputs 一律用 `TerminalDisplayStack` 在 `CUSTOM_DATA` 的 reserved marker 同步 exact non-negative `long` amount，實際 stack count 固定為 1 以保留零庫存 icon。`TerminalDisplayStack.amount` 是排序/畫字/tooltip 唯一來源；零不畫 overlay，tooltip 仍顯示 exact stored amount。Non-item/addon entries另用`TerminalResourceDisplay`攜帶exact kind/resource/variant key；它們只是代表物，pickup/shift-click必須no-op，不可把bucket、redstone或addon icon當真實物品抽出。`ItemKey.of`、recipe selection、EMI inventory view 與實際 storage/output 邊界必須 strip marker；EMI view 另外把 logical amount 傳給 `EmiStack`，不可把 marker 當成 recipe component或把庫存誤報為 1。Craftable amount 是目前 Core 庫存，不是 potential craft yield；零庫存可選/可合成但不可抽取。Name/Quantity/Mod/ID 都走同一 `TerminalEntryComparator`，component variants 必須有 deterministic final tie-breaker。
- **搜尋封包邊界**:`SearchFilterPacket` 最多接受 64 UTF-8 chars(GUI EditBox 目前 50);server tag filter 必須用 `ResourceLocation.tryParse`,畸形 `#tag` 只是不匹配,不可讓惡意封包拋 `ResourceLocationException`。
- **捲動封包是 absolute + server validated**:`TerminalScrollPacket` 傳 `{containerId,offset}`；server 只接受目前玩家開啟、container id 相同的 `StorageTerminalMenu`，再 clamp 到可見範圍。client drag 只在 absolute target 改變時送一包，不可用 button loop、固定 sleep 或強制 row-snap。screen re-init 必須保留搜尋文字、焦點與 debounce deadline。
- **Patchouli 1.20+ mod book 路徑**:`book.json` 留在 `data/magic_storage/patchouli_books/guide/book.json`,但 categories/entries/templates 必須放 `assets/magic_storage/patchouli_books/guide/en_us/...`;放在 `data/.../en_us` 會導致 `BookContentResourceListenerLoader preloaded 0 jsons` 與書內 `No Entries`。
- **Menu data-slot client/server parity**:server(core 建構子)與 client(buf 建構子的 data/slot 數量必須**完全一致**,否則 container sync 會崩潰。Storage data固定13，index 11是type-capacity explicit-unlimited bit、index 12是server-owned resource view；Crafting額外89，總data slots固定102，其中Crafting index 7是direct-terminal output destination，live energy後保留4個永遠讀零/忽略寫入的retired data values，末五格是Axe Energy四個unsigned-16 words與infinite flag。Crafting slots固定為 display `0..80`、player `81..116`、fuel input `117`、persistent process/instant slots `118..125`、transient Axe Energy input `126`、presentation metadata `127..148`，兩個 constructor 都恰好149 slots。Hidden presentation slots依序是exact output、9個item ledger representatives、station、9個positioned inputs、tool與最後寫入的typed metadata carrier；carrier另含tool-resource explicit infinity bit，不能以`Long.MAX_VALUE`猜測。Client可能在各slot packet之間render；metadata已到但resource/tool slot尚未到時必須暫時回empty presentation，不能crash或把不完整snapshot當有效。全部offscreen/inactive/reject place/pickup。EMI在Storage只可使用真正item display + optional player，typed代表物不得當item source；Craftable synthetic display不可當ingredient source，Fuel全拒絕。
- **Menu open buffer contract**:storage/crafting terminal 的 buf 依序寫 `corePos`、`accessPos`、`remoteAccess`。Server menu 另外在建構時 pin Core UUID + dimension(不存 client storage state);本地 terminal 必須仍在 `connectedBlocks`,remote 雖不受距離限制也不可跨維度、切到同座標 replacement Core 或用 `getBlockEntity` 偷載 unloaded chunk。新增 buf 欄位時兩端 constructor 與 parity GameTest 要一起改。
- **Storage API / live menu contract**:Item、fluid、FE、chemical與addon kind的live amount只存在同一`StorageResourceLedger`；`ItemKey`是exact bridge，segmented item NBT只作persistence compatibility，禁止再加第二個runtime item map。Public addon註冊走`StorageResourceKindApi`，canonical built-ins走`StorageResourceKey.item/fluid/neoforgeEnergy`，multi-key mutation走`StorageResourceTransaction`。一般 stack mutation API 是 `insertItem(stack, Action, Actor)` / `extractItem(key, amount, Action, Actor)` / `extractMatching(predicate, amount, Action, Actor)`；合成的大額 per-key transaction 改用 `insertItemCount(key, long, Action, Actor)` / `extractItemCount(key, long, Action, Actor)`，不得用數十億次 int chunk loop。種類容量的authoritative值是`StorageTypeCapacity(finiteTypeSlots, unlimited)`；`getTotalTypeSlots()`只保留有限component的legacy查詢，任何新type admission與craft output planning都必須使用explicit predicate。容量由topology導出，不另存NBT；full rebuild與incremental add共用composition，容量record變化會提升topology revision。`SIMULATE`不改storage/不fire event，`EXECUTE`才setChanged + 一次`onChanged`。Machine tick/consume、Fuel conversion、Axe Energy conversion/consume另fire`onEnergyChanged`，open menu在`broadcastChanges`刷新grid/preview。一次合成commit必須包在`beginMutationBatch`/`endMutationBatch`，把listener callback延到驗證、扣料、扣process/Fuel/Axe Energy與rollback邊界之後；否則reentrant refresh/conflict可能在兩個ingredient extraction中間打斷交易。玩家36格fingerprint、Core topology revision/conflict改變也要讓preview即時失效重算。
- **連線檢查不可載入 chunk**:Terminal/Import Bus/Export Bus 可 cache `access → Core` path，但每次使用前都要確認 path 仍連續、每格 chunk 已載入且仍是 network block；失效時只可用 `findLoadedNetworkPath` 尋找另一條已載入路徑。不可用 `getBlockEntity` 或一般 `getChunk` 為連線驗證偷載區塊，也不可只驗 path 兩端。
- **ItemKey 必須是真正 immutable snapshot**:`ItemKey.of` 要複製目前 `DataComponentMap`，不可直接保留來源 `ItemStack.getComponents()` 的 mutable patch view；來源 stack 後續 mutation 不得改變 map key 的 equals/hashCode。
- **燃料轉換是 server-authoritative Fuel-page action**:玩家選項只有 `Auto`/Fuel/Brew Energy；內部 `furnace_fuel` ID 不遷移。Fuel 不使用 item whitelist：slot 只做早期 UX，Core commit 重新呼叫目前 stack 的 `getBurnTime(null)`，正值才接受並精確按 burn ticks 加值，所以 logs/modded/data-map fuel 自然支援。`Auto` 首選 overflow 不得 fallback；失敗、換頁或關閉不得吞物。Installed machines 不因換頁/close 歸還。max-one instant station只接受一個並把remainder留在來源stack；Axe Energy只處理本次server驗證的transient input。本次repository格式不讀舊machine slot或legacy axe payload。
- **Bottle Energy 已退役且不遷移**:`BOTTLE_FUEL`/FuelTable mappings/lang/GUI target/Patchouli/test-world baseline與Core migration surface全部移除；現行repository schema不讀`bottle_fuel`或`legacyBottleFuel`。Crafting menu只保留retired 4-word zero data region；加上現行base unlimited/resource-view同步後總data-slot parity為102。
- **Cooking recipe cost 取 concrete recipe time**:exact cooking adapter 以每次從目前 server `RecipeManager` 重解析並重新 classify 的 holder `getCookingTime()`，同時計算 matching process pool 與 Fuel；preview/execute/EMI request 共用該 immutable match cost，非正值/stale/unsupported 不可補預設值。
- **Craftable mode 的候選不是 storage outputs**:`CraftableRecipeCatalog` 從目前 server recipes 與可用材料索引 candidates，再以目前材料/能量完整 simulate 後顯示；zero-storage Charcoal 可出現。Synthetic output 可選 exact recipe，但所有 extraction/Shift/pick/drop 路徑都 no-op。
- **Client crafting screen 不做 server 邏輯**:`CraftingTerminalScreen` 不掃 `RecipeManager`、不直接讀 core storage/energy；只讀 menu 重建的 immutable `RecipePresentation`。Exact recipe ID/kind、shaped dimensions/positions、shapeless state、output components/count、station、item/process/Fuel/tool available-required rows都由 server data/hidden slots 同步；tool resource 額外同步 explicit infinity bit，畫面只有該 bit 為 true 才顯示 `∞`，有限 `Long.MAX_VALUE` 仍是有限數。Selection clear 時保留 selected output identity，才能區分「尚未選擇」與中性 `No supported recipe`；empty presentation必須把diagram+ledger union當成單一中性prompt card。有效/空白recipe body都必須位於footer上方；ledger required resources由頂端對齊、最多四欄，超過八種時使用第三列，目前wire上限可容納總計十二種item/energy/tool resources；不可讓fullscreen高度變成大型無語意空區。有效 presentation 的 station badge 只由 shared screen 畫一次、固定在 diagram 右下並加暗色 overlay，native/EMI renderer不得重複畫；native slots使用vanilla inset slot frame，navigation與craft actions使用vanilla-style buttons。Craft Output item icon 必須依 server-synced destination 即時切換 player head/Storage Core。Wire regression 會把真實 signed-short data 與全部 hidden stacks複製到 client constructor 後再驗 exact model。Screen 只連 Magic Storage `RecipeDiagramRenderer`；dev compile/runtime基準是EMI `1.1.24+1.21.1`，release client dependency range是`[1.1.24,2)`且dedicated server不要求EMI。`ModList` guard 後的 compat adapter 只使用公開 `EmiApi`/`EmiRecipe`/`Widget`/`WidgetHolder`，exact ID/backing holder 必須一致，widget bounds 必須位於 holder/diagram；沒有 public representation才用native，runtime exception必須直接暴露，不可broad catch後靜默native。Ingredient predicate 只有 `equals` 相同才可合併；顯示 item 相同但 predicate 不同仍分列。SelfTest/GameTest 在 dedicated server 跑,client-only 類別仍不可被 server test 直接引用;Python static regressions會守護這點。Runtime `en_us`/`zh_tw` lang key set 必須完全相同。
- **合成 recipe/station contract / reload**:終端內建 exact concrete `ShapedRecipe`、`ShapelessRecipe`、`StonecutterRecipe`、`SmeltingRecipe`、`BlastingRecipe`、`SmokingRecipe`、`CampfireCookingRecipe`、`SmithingTransformRecipe`、`SmithingTrimRecipe`，加上 internal synthetic axe strip/scrape/wax-off；任何namespace只要仍是同一exact class/type就零設定沿用。Custom exact class/type可在正常NeoForge registry lifecycle經`RecipeFamilyApi.createDeferredRegister(modId)`註冊；公開factory包含bounded `singleItemToItem`與`deterministicResources`。後者的`TypedRecipePlan`必須有1–9個exact inputs、至少一個primary item output、explicit consume/catalyst/tool與primary/remainder roles；consume乘craft數，catalyst/tool整批retained，non-item outputs回Core。Public energy cost不得為負、不得兩欄同時為零，也不得讓兩個正值指向同一pool；零成本必須使用`free()`。外部family固定排built-ins後再按完整registry ID；missing station descriptor、duplicate ID或duplicate exact class/type直接啟動失敗，不能shadow。`RecipeFamily`保持opaque，不公開`RecipeAdapterMatch`、Core、玩家、allocation或commit callback；registry freeze後不能hot register。每個內部 match 仍必填 ordered input predicates/multiplicity、stable station descriptor、process/Fuel/tool cost、checked exact output/remainders、presentation、variant與simulation/commit validators，不存在reflection、名稱、serializer JSON、EMI widget或`Recipe#getIngredients()`generic fallback。`CraftingTerminalMenu`只保留通用joint reservation、destination delivery、capacity planning與atomic mutation；所有Core item/non-item ingredient/output先合成一張`StorageResourceTransaction`再單次simulate→commit。Smithing template/base/addition直接委派原recipe predicate並綁exact來源stack；Transform/Trim都呼叫exact `assemble`且Trim要求registry mapping。同ID多variant從目前server holder按selected exact output重解；stale holder、variant消失、missing station/context或cap超限皆fail closed。Cooking用concrete cooking time；axe每craft一單位finite/∞ tool cost且只有menu可扣Core。Axe catalog只掃`minecraft` namespace default state，不用fabricated player/context呼叫任意mod hook。Blocked output/ingredient不扣energy/tool。不同`Ingredient` predicate不可按display signature合併；player inventory只掃36格。Max與long-count reservation/rollback維持checked。完整addon契約與範例見`docs/recipe-family-api.md`。
- **Direct output destination 與 EMI destination 必須分離**:`TerminalOutputDestination` 只控制 terminal 自己的 `×1/×8/×64/Max`，預設 Player，且與 `use player inventory` ingredient source 是兩個獨立 server-synced state。Player 在扣完 player-source ingredients 後先填玩家 36 格、overflow 回 Core；Storage 的 primary outputs 與 crafting remainders 全部只進 Core。完整批次的 type/long capacity 必須先驗證，少一件容量也整筆 no-op；commit 中途拒收則 rollback output 與 Core ingredients。EMI `CraftingDestination.NONE/CURSOR/INVENTORY` 仍是每個 exact request 的權威，不得被 terminal toggle 覆寫。
- **合成 output transaction 不得掉落/吞掉**:INVENTORY destination 先在扣除本次 player ingredients 的 36 格 snapshot 中放 primary output + container remainders，剩餘全規劃回 Core；若 post-extraction Core type/counter capacity 不能完整接受，就在任何 mutation 前拒絕整批。Commit必須重新驗player/Core/station/Axe Energy/typed plan identity；Core ingredients、Core outputs與typed deltas以單一ledger transaction提交，再扣process/Fuel/Axe Energy並套用player inventory/cursor；任何拒絕都不可留下部分Core delta。Requested output 沒有 world-drop fallback，兩邊都滿就是 no-op。CURSOR 只接受單一 exact identity 且不可覆蓋 incompatible carried stack。
- **EMI dependency / compatibility contract**:EMI是release的client-only required dependency，Maven range固定`[1.1.24,2)`；`emi_version=1.1.24+1.21.1`與matching `emi_runtime_version=5sIPA1To`只代表reproducible minimum compile/dev baseline，不得寫成玩家端exact pin。Compile-only與隔離的client/data runtime都使用`maven.modrinth:emi:<version-id>`；compileOnly不會把EMI帶入dedicated runtime，plain `runtimeOnly`仍會污染dedicated GameTest，禁止使用。`scripts/resolve_emi_version.py`直接查官方Modrinth versions API，只接受同Minecraft 1.21.1、NeoForge loader、listed release、在inclusive-min/exclusive-max範圍內且含唯一positive-size primary non-API jar的numeric release；`scripts/resolve_emi_runtime.py <release-version>`再把同一exact `+neoforge` release映射到immutable version ID，缺少或多筆都fail closed。CI與release為minimum及最新相容release各自傳入匹配的human-readable version與Modrinth ID後編譯；client-smoke也以同一映射取得最新runtime。Full jar由`bash scripts/stage_emi_runtime.sh <release-version> <modrinth-version-id>`走Gradle Modrinth Maven落在`build/client-smoke-mods/emi-neoforge-<release-version>.jar`，不得direct curl或直接寫共用`run/mods`；同一Gradle command只可重試一次。解析、映射、下載、ZIP驗證或編譯任一步失敗都直接fail，不得換來源或退回baseline假裝latest通過。Integration只能import`dev.emi.emi.api`公開契約；dedicated server不要求EMI。
- **EMI one-level magic crafting 綁 exact backing identity**:client gate先以backing recipe ID解析目前client holder，要求exact holder identity一致並重新classify；packet再送exact `{containerId,recipeId,amount,destination}`。NONE只選取，CURSOR/INVENTORY由server再次解析目前holder並由adapter重算station/Axe Energy/材料/實際cooking cost，並以「假想扣除本次 player ingredients 後」的 destination capacity 選不超過 request 的最大完整 recipe 數，再 atomic commit。不得信任 client craftability、不得換成同輸出第一個 recipe；Trim必須再綁selected exact output variant，special/dynamic/incomplete或缺少registry context仍fail closed。這是單層立即合成，不包含 RS2 recursive pattern tree，也不為 EMI synthetic favorites 同步整個 network inventory。
- **EMI recipe tree 不是server planner API**:目前最低支援EMI 1.1.24公開API可查input/output recipes、判斷`supportsRecipeTree()`與開啟tree screen，但建立/讀取goal使用runtime-only internal `BoM`/`MaterialTree`，API jar沒有這些類別。禁止直接link、reflection或把client tree當authority。多步驟合成依`docs/superpowers/plans/2026-07-14-modded-recipes-multistep-crafting.md`建立server adapter graph；EMI只作client recipe/tree UI。

## Decisions

- **統一能量池模型**:所有能量池同為 `Map<EnergyType, Long>` 無上限,差異只在來源,降低特例。
- **參考 RS2 而非依賴**:借設計模式,不加為依賴;加任何依賴前先問使用者(minimize deps)。
- **絕不整段照抄 RS2**:授權不同;只取模式自行實作。
- **一網一 core(multi-core 不支援)**:`rebuildNetwork` BFS 碰到第二個 core 即設 `conflicted` → 該 core 停止接受物品 + log 警告,避免雙重容量/非決定性;`isConflicted()` 可查、`tryIncrementalAdd` 在 conflicted 時退回 full rebuild。
- **Bus 吞吐 vs 省資源**:Import/Export bus每個cooldown(10 tick)最多搬一整疊item(≤64)，並各嘗試一次bounded typed transfer(≤1000 native units)；Export只抽目標模擬可接收量。active/passive共用missing-Core 10-tick negative cache，automation probe不可每次重跑最高8192-block BFS。Item在reentrant capacity改變時依actual remainder精確退回，仍拒收才顯式drop；typed overdraw/remainder先還source/Core，仍無法歸還則進persistent Bus escrow並暫停能力，絕不忽略回傳值吞資源。
- **Bus cached core 必須驗證仍連通**:Import/Export bus 的 `cachedCore` 只有在 `core.getConnectedBlocks().contains(busPos)` 時可重用;中間 unit 被拆後不能靠舊 reference 繼續搬物。

## Hardening Pass (2026-06-21)

地毯式 bug 掃描 + 修正。關鍵點與借鏡的 RS2/AE2 模式(僅取模式,未照抄):

- **持久化**:`StorageCoreBlockEntity`只在storage reference改變時把chunk標記dirty；inventory、energy、machine與descriptor mutation直接更新attached `CoreStorageRecord`並讓overworld `CoreStorageRepository` dirty，outer mutation batch只mark一次。BE NBT固定只含`storageId`/`storageSchema`；完整`ItemStack` components與positive `long` count在world data中以每段最多63種的`inventorySegments`保存。拆除只新增recovery UUID→同一storage UUID的indirection，packed item沒有`BLOCK_ENTITY_DATA`或payload fallback。empty attached Core完成移除才刪record；non-empty、missing、corrupt、packed或duplicate狀態絕不建立空白replacement。
- **Core 身分 / remote 綁定**:repository record持有永久storage UUID與network UUID，BE只持有storage UUID/schema reference；packed token只指向同一record。Storage Core不要求特定工具才掉落。Remote Terminal綁`{network UUID,dimension,pos}`並只接受該位置仍解析到同一可用Core；舊coordinate-only binding必須fail closed要求重綁，不可因同座標新Core而靜默存取另一網路。`RemoteTerminalItem.isBound`必須驗齊x/y/z/dimension/UUID並用`tryParse`；重新綁定用`CustomData.update`只覆寫自己的keys。
- **匯流排/合成原子性**:Import bus active pull、passive capability insert 與 `craftItem` 都先 simulate/reserve 再 commit。Passive caller stack 不可 mutation，execute 只 commit simulate 可接受的數量並回 exact remainder。相同 predicate 也保留獨立需求；不同但重疊的 predicates(例如 `#planks` + oak-only，或 strict/partial component predicates)用 joint reservation/max-flow,同一個 item unit 不可算兩次,且要能把 broad match 重新路由給其他變體。conflicted Core、unsupported/dynamic/incomplete recipe 一律 preview 0/拒絕 execute;材料 commit 失敗要 rollback。物品級 crafting remainder(如 milk bucket→bucket)交還玩家。
- **insert/extract 契約**:`insertItem(stack, simulate)` 回傳實際接受量、`extractItem` 回傳實際取出量且庫存只減該量;呼叫端必須尊重回傳值(勿丟棄剩餘 → 否則網路滿時吃物)。
- **數值安全**:`consumeEnergy` / fuel 計算用 exact arithmetic；machine-generated energy 在 `Long.MAX_VALUE` 飽和，load 時負 energy 夾 0。item insert、NBT duplicate merge、fuzzy count 與 Core+玩家 preview 都必須 saturation，不得 wrap。
- **GUI**:隱藏的 `GhostSlot` 以 `isActive()` 停用。Vanilla 在 callback 後才設定 button focus，所以 callback 內清 focus 無效；Crafting screen 必須在 `super.mouseClicked(...)` 返回後清 `AbstractButton` focus並保留 search `EditBox`。Fuel 常駐 panel 不顯示 rate 公式或每格名稱，centered values 必須 `drawString(..., false)` 無 shadow，精確 identity/rate/total 放 tooltip；tooltip只在實際 station slot或reserve icon bounds觸發。Fuel target popup只用 `addWidget` 接收 input，再於 `super.render` 後的 foreground pass 繪製；覆蓋區必須先攔截click/scroll/tooltip，不能被 container foreground 蓋住或穿透到container slot。因 Slot.x/y final，client 重建 fuel/machine slots時要委派原 slot validation；off-page machine slots 放 `-9999,-9999`，panel-local wheel 切頁後重建。
- **網路**:所有 network block 都以 block callback 為 authoritative path：`onRemove` 在 state 已替換後重建 old position，`moved=true` 也不可跳過；`onPlace` 將一般、指令/`setBlock`、活塞 final state 合併到 next-tick growth pass，批次放置同 component 只重建受影響 Core。不可只靠 `BreakEvent`/`EntityPlaceEvent`。`tryIncrementalAdd` 先用 cached connected set 守住與 full rebuild 相同的 depth/member boundary；bridge detached segment、多 Core、破壞仍 full rebuild。Chunk lifecycle 另外以 server-only FULL ticket transition 驅動：失去 FULL 時只先保留既有 `LevelChunk` snapshot，下一 tick 才從 snapshot 的 network boundary 與仍 loaded 的外側 bounded BFS 找 Core 並 full rebuild；`ChunkEvent.Load` 只接受 server `LevelChunk` 並 one-shot 排程，所有 level interaction 延到下一 tick，flush 以 non-loading `getChunkNow` 試一次後一律移除 queue entry，未就緒不逐 tick retry，後續 FULL ticket transition 再補排；同一 flush window 內未實際卸載就重新取得 FULL 時取消 pending unload，不額外重掃。Boundary 會先篩出兩側都仍是 loaded network block 的有效連接，再套用 `MAX_NETWORK_BLOCKS` 上限；絕不 force-load、也不做每 tick/週期性整網掃描。Unload 會自動移除遠端容量，reload 會自動恢復，multi-Core conflict 與完整拓樸語意不變。`bfsFindCore` / `bfsFindCorePositions` / full `rebuildNetwork` 都受 `NETWORK_SCAN_DEPTH` + `MAX_NETWORK_BLOCKS` 上限保護;封包 handler 包 `ctx.enqueueWork(...)` 並透過 menu 的 exact-Core resolver。

## RS2 Heuristics Adopted (2026-06-21)

完整對照見 `docs/rs2-design-gap.md`(該採用 vs 故意分歧)+ 計劃 `docs/superpowers/plans/2026-06-21-rs2-heuristics-adoption.md`。已採用(僅取模式):

- **fuzzy 配對(A5b)**:合成材料以 `Ingredient.test` 跨變體;preview/execute 共用 joint reservation plan,最後按實際 `ItemKey` 精確扣除(取代各 predicate 獨立 `countMatching` 後重複算同一批物品)。
- **Action/Actor + change events(A2/A3 foundation)**:storage mutation 都可帶 `Action` 與 `Actor`;execute insert/extract 會發 `StorageListener` delta event。P3 若要做 grid delta,應基於此 listener,不要再從 client 保存 storage state。
- **view 設定 server 同步(A5a)**:sort / order / search 模式為 server 權威、經 data slot 同步;終端按鈕標籤/tooltip 反映真實狀態(非 client 私有)。
- **選取物品身分化(A4)**:crafting 選取以 `ItemKey` 身分為準(經畫面外 `selectionContainer` slot 自動同步),非 grid slot index → 排序/捲動後配方面板不再亂指。
- **合成 preview(A7)**:用 simulate 路徑算「可做 N / 缺什麼」顯示,不異動狀態。
- **增量網路成長(A1,安全範圍)**:放置 callback 先 coalesce 到 next tick；單一方塊接到既有 connected set 時，`StorageCoreBlockEntity.tryIncrementalAdd` 用 cached-set bounded path check 確認不越過 full rebuild depth/member guard 後加入(不讀整個 world graph)。**破壞/批次/不確定拓樸一律 full `rebuildNetwork`**(移除可能分裂網路,放置若 bridge detached segment 或連到多個 core 也屬不確定)。`capacityOf()` 是兩條路徑唯一的容量計算來源;GameTest 守護 simple growth、programmatic placement、piston move、depth boundary、detached bridge full rebuild、multi-core bridge conflicted。
- **仍未採用(未來)**:**P3 增量 grid delta**(grid 改收差異、不整份重建)與完整常駐 network graph;目前分頁式 grid(≤81 格,vanilla 已增量同步)低價值,除非改成整列表 client grid 才值得。
