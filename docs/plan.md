# Plan

> 完整里程碑與設計見 `PLAN.md`(§八 開發里程碑 M1–M12)。狀態以程式碼為準(已有 50 Java 檔 + SelfTest/GameTest)。

M1–M11 大致到位。0.1.15 station/tool gates、expanded recipes、safe output、two-row Fuel layout 與 16×16 texture revision 已完成 focused RED→GREEN 與 final automated gates；deployment/fullscreen verdict 依使用者要求延後。

## In Progress

- **0.1.15 visual/deployment gate**：final `build`、GameTest（SelfTest 40608 + GameTest 222）、Python 72/72、`runData`、JSON/datagen drift 與 pre-push mechanical checks 已完成。Prism exact-jar deployment 與 fullscreen checklist 由使用者本輪明確延後，因此 commit/push 不代表 Installed Stations 兩列、右下 Fuel control panel、recipe workspace、focus/tooltips 或新 16×16 textures 已目視通過。不可沿用 0.1.14 handoff 或標記 GUI verified。

## Next Up

- **Core item 大型 NBT redesign**:目前正確工具掉落會攜帶完整庫存;極多 component-heavy variants 可能撞 vanilla 約 2 MiB NBT/network 上限。不可用截斷/掉資料解法;需要 UUID + server SavedData/可回收 ownership 的明確設計後再做。
- **更多 context-dependent recipes**:exact `SmithingTransformRecipe` 與 deterministic axe strip/scrape/wax-off 已完成；Smithing Trim、special/dynamic recipe subclasses、event/player/world-state-dependent transformations、loom/cartography/grindstone/anvil/enchanting 仍 fail closed。只有能 server-authoritatively 建模完整 input/output/components/side effects 才可逐類加入。
- **Brewing integration**:`BOTTLE_FUEL`/`BREW_ENERGY` 已可儲能但尚無 production recipe consumer;不要把「可轉 bottle fuel」誤當 brewing 已完成。
- **P3 增量 grid delta(延後)**:A3 foundation 已完成;目前分頁式 grid(≤81 格,vanilla slot sync 已是增量)價值偏低,除非改成整列表 client grid 才值得做。
- **剩餘一般 deprecation(低優先)**:`ClientSetup` 的 for-removal event-bus warning 已消除；`-Xlint:deprecation` 仍列出 production `Level.hasChunkAt`、item registry holder 查詢，以及 GameTest `RegistryFriendlyByteBuf` 舊建構子。現行 NeoForge 21.1.228 可用且 tests 全綠，後續做純 API migration 時要保持 unloaded-chunk fail-closed 行為。

## Latest Verification

- 2026-07-13 0.1.15 focused TDD：full/partial inventory、Core overflow/full rejection、direct/EMI/Max/long-count/largest-deliverable output 路徑；9-slot station/tool gating；component-exact/preserving Smithing Transform；vanilla axe strip/scrape/wax-off + raw durability + blocked-output atomicity；two-row/full-width Fuel geometry + occupied lower-right control panel；model-referenced 16×16 texture static contract均已 RED→GREEN。Final versioned automated gates為 build、SelfTest 40608 + GameTest 222、Python 72/72、runData、JSON parse 與 datagen no-drift；deployment/fullscreen handoff 由使用者延後。Current contract：`docs/superpowers/plans/2026-07-13-stations-recipes-output-textures.md`。
- 2026-07-13 0.1.14 Prism handoff：`PYTHONDONTWRITEBYTECODE=1 python3 scripts/run_prism_gui_session.py --scenario crafting-fuel-page` 成功，`build/gui-runs/20260713-141850-crafting-fuel-page/` 含 manifest/session/checklist/log excerpt；current-run log 證明 Magic Storage 0.1.14、SelfTest 40406/40406、`MS_GUI_TEST_READY` 與 offline player `MagicStorageBot`。已通過自動 READY gate並保留 Minecraft，fullscreen 視覺 checklist 仍由使用者執行與判定。
- 2026-07-13 Task 9 Fuel selector/layout：先以 missing selector/rail geometry 的 5 個 compile errors、3 個 static contracts、Prism checklist 確認 RED；client layout 完成後，再以 GameTest 證明 retired ID 17 確實改動 target。Production 使用 header vanilla `CycleButton`、Fuel rail 僅三頁籤、server 僅 Auto/exact IDs、page-aware proportional flow cells，且 selector wheel 優先於 reserve paging。`compileJava`、`build`、`runGameTestServer`（SelfTest 40406 + GameTest 210）、Python 70/70、`runData`、77 JSON parse 與 datagen no-new-drift 全綠；final 0.1.14 取代測試強化前的 0.1.13 intermediate，唯一 Prism jar 與 build jar SHA-256 同為 `97069ca985797ce237b421df2fc5c08c44cc2ca4dedf1957d0f756c2c92dae75`。只剩 fullscreen gate。
- 2026-07-13 0.1.12 fullscreen follow-up：代表物/compact amount 自動 gate 已完成，但實機否決 sparse descriptors 左聚集與 previous/next/current target 顯示語意。當時 Task 9 contract 改成 CycleButton current-value selector、compact Fuel rail、balanced flow，後續已由上方 0.1.14 verification 完成。
- 2026-07-13 Task 8 representative amount/type-capacity revision：先以 missing formatter/representative APIs 與 Python contracts 確認 RED；再新增完整八種代表物、compact K/M/G/T/P/E、16px normal/0.5-scale cell-local right alignment、Fuel type-capacity wire parity。額外 GameTest 以 `menu=10/core=35` 重現 open Fuel 頁 topology capacity stale，再修成 topology revision 變更即 refresh metadata。`build`、`runGameTestServer`（SelfTest 40340 + GameTest 209）、Python 70/70、`runData`、77 JSON parse 與 datagen no-new-drift 全綠；deploy 自動 bump 0.1.11→0.1.12，唯一 Prism jar 與 build jar SHA-256 同為 `f7b0e78e630c03f77331fa9aad4c4d3f04512d069b327c9bad6942a0c8844309`。只剩 fullscreen gate。
- 2026-07-12 Task 7 three-page/scalable Fuel revision：先確認 Python checklist/static 與 compileJava 因缺少新契約而 RED，再完成 Storage/Craftable/Fuel、94 data-slot parity、synthetic extraction/EMI source boundary、fresh uncapped Max、live exact-button state、grouped fixed-width target rail、full-width descriptor FlowGrid/paging、shadowless values 與 semantic search icons。額外 RED 證明 10,005 次合法 craft 不可被 9,999 wire preview 截斷，且 60 reserve descriptors 不可讓 rail 出界。`compileJava`、`build`、`runGameTestServer`（SelfTest 40321 + GameTest 208）、Python 69/69、`runData`、77 JSON parse 與 datagen drift checks 全綠；0.1.11 同版本最終 jar transactionally 取代舊部署，唯一 Prism jar 與 build jar SHA-256 同為 `09fd5ca4e2b2d41c1163daa255da58232be021f1a131f30e53d80748f37b6d1d`。只剩 fullscreen gate。
- 2026-07-12 Task 6 adaptive/resource revision：先加入會失敗的 ingredient/process/Fuel preview、full-`ItemKey`、Compact removal、95-slot signed-short parity、complete-geometry margins/centering/resource cells 與 fixed icon canvas tests，再完成最小 production 實作。`compileJava`、`build`、SelfTest 39823 + GameTest 205、Python 68/68、`runData`、77 JSON parse、diff/drift checks 全綠；deploy 自動 bump 0.1.9→0.1.10，Prism 唯一 jar 與 build jar SHA-256 同為 `2697c631ead5351af5bed7803656f1b1a80f3c0aed83e496e2a8087090afd59b`。只剩 fullscreen gate。
- 2026-07-12 Dynamic Fuel/Craftable/EMI/adaptive GUI 0.1.9 gate：`./gradlew compileJava`、`./gradlew build`、`./gradlew runGameTestServer`（SelfTest 6610 + GameTest 204）、`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover scripts`（66/66）與 `./gradlew runData` 全綠。Deploy script 自動 bump 0.1.8→0.1.9，Prism mods 只有 `magic_storage-0.1.9.jar`，與 build jar SHA-256 同為 `22f20cc3f38d8bee3160cf91be8c32d8d4faa9ed9582bc89a02cb1a6ce00d858`。新增 runtime oak-log Fuel、concrete cookingtime preview/execute、zero-storage recipe catalog/reload/search/sort、EMI exact ID/amount/destination/atomic capacity、320×240 到 600 高度 exhaustive geometry、original semantic slot delegates、vanilla-like palette、focus cleanup 與 EMI exclusion regressions；新 jar fullscreen gate 尚待使用者執行。
- 2026-07-12 Fuel/machine + deep correctness gate:`./gradlew build --console=plain --no-daemon`、`./gradlew runGameTestServer --console=plain --no-daemon`（SelfTest 117 + GameTest 180）、`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover scripts`（61/61）與 `./gradlew runData --console=plain --no-daemon`（resource snapshot 無 drift）全綠。新增/守護:ItemKey component snapshot、Locale.ROOT/compact identity ordering、display click/shift extraction whitelist、11/52 signed-short wire parity、absolute scroll packet、搜尋 state re-init、EMI player-source toggle、live player/topology preview、currently-loaded cached path、deferred-listener atomic craft commit、unknown/no-op packet 去重與 preview 一次 source snapshot；GUI world/Prism runner/deploy 的 staging、hash、rollback、runner-owned process cleanup 與 primary-monitor actionable failure 也有故障注入測試。部署器已自動升至 0.1.8，Prism mods 只留一個且與 build jar SHA-256 相同。
- 2026-07-11 second correctness pass:`./gradlew runGameTestServer --console=plain --no-daemon` 已驗證 SelfTest 104 + GameTest 141。新增/守護:lava bucket 歸還空桶、fuzzy matching / NBT duplicate / Core+player preview long 飽和、EMI 只傳 exact supported backing recipe id 且 server 重驗、Smithing fail closed、活塞 old/new topology、程式化放置 coalesced next-tick growth、增量/full-rebuild depth parity，以及 Import/Export bus 在 reentrant commit/rollback 改變時精確歸還或顯式掉出 remainder。
- 2026-07-11 full correctness audit:`./gradlew runGameTestServer --console=plain --no-daemon` 已驗證 SelfTest 104 + GameTest 131。新增/守護:Core drop/NBT identity persistence、long item/energy saturation、non-player removal rebuild、open terminal live refresh/scroll sync/disconnect、search packet/tag validation、legal shift extraction、conflicted/incomplete recipe rejection、overlapping ingredient joint reservation + reroute、crafting container remainder、terminal fuel popup/server validation/preview refresh、Remote exact-Core UUID/dimension pinning與 legacy binding fail-closed。
- 2026-07-12 Prism GUI manual handoff hardening:`scripts/setup_prism_computer_use_wrapper.py` 只保留作選用診斷；正常 `scripts/run_prism_gui_session.py` 強制 `WrapperCommand=` + `OverrideConsole=true` + `ShowConsole=false` + `ShowConsoleOnError=false`，產生 `build/gui-runs/<timestamp>-<scenario>/` artifacts、以 native Prism offline player `-o MagicStorageBot` 跳過 Microsoft/Xbox account refresh、用 offset+inode cursor 只讀本次 log、區分 `boot-smoke`(不需目視)與 visual scenarios。Visual scenario 到 `MS_GUI_TEST_READY` 後明確輸出 handoff message 並停止自動 GUI 操作，由使用者完成 fullscreen gate/checklist。offline UUID 仍可能把 profile-properties 401 寫進檔案，但不再自動彈 Prism console；runner 只白名單此已知 stack，其他 ERROR/Caused by 仍 fail。21:04 舊 wrapper run 已確認於 21:10 `Stopping!` 後退出，不可拿舊 schema artifact 當新流程證據。
- 2026-07-10 P0/P1/P2 RS2 parity + test expansion:`./gradlew compileJava`、`./gradlew runGameTestServer` 已驗證 SelfTest 104 + GameTest 102。新增/守護:Action/Actor storage ops、per-resource listener events、simulate no event、type-slot reuse、bus cached-core disconnect invalidation、import bus type-full no extraction、export respects target capacity、invalid tag filter no throw、incremental placement bridge fallback、multi-core bridge conflicted、terminal access/remote `stillValid`、display pickup stack amounts、craftable-only server filter、server-synced crafting recipe metadata/hidden slots parity、EMI no hidden metadata slots、real crafting/smelting/player-inventory craft paths、duplicate ingredients aggregate before craft；當時的初始 compact grid 後來已由 Task 6 完整移除。
- 2026-07-10 ClientSetup NeoForge deprecation cleanup:`@EventBusSubscriber(bus = EventBusSubscriber.Bus.MOD)` 已移除,client-only `RegisterMenuScreensEvent` 改由 `FMLEnvironment.dist == Dist.CLIENT` guard 後用 `modEventBus.addListener(ClientSetup::registerScreens)` 註冊;Python static regression 守護不回退。
- 2026-07-09 GUI test world 固定流程:`python3 scripts/prepare_prism_gui_world.py` 會重建 Prism dev `MagicStorageGuiTest`、安裝無 command block datapack rig、啟用 `/function`/`/tp`、固定座標與 `u` 開啟方塊,後續 Computer Use GUI 測試改採當次需求動態選 hotbar view key + mandatory fullscreen gate + condition polling;已實測 `1`/`2` 對準並在 fullscreen 下開 Storage/Crafting Terminal。
- 2026-07-09 CI/CD 本地驗證目標:`.github/workflows/ci.yml` 跑 build + GameTest + Python unittest + datagen drift check + jar/log/report artifacts;`.github/workflows/release.yml` 僅接受 tag `v<mod_version>`,重跑同一組 gates,產 release notes 並用 GitHub CLI 建 release。
- 2026-07-08 Prism dev 實機驗證:`magic_storage-0.1.3.jar` 已部署到 dev instance,mods 內僅此一個 Magic Storage jar。以 `open -a "Prism Launcher" --args -l dev -w "New World"` 直進世界(現已由 `MagicStorageGuiTest` 固定流程取代),先 windowed 再切 macOS native fullscreen;Computer Use 目視確認 Patchouli guide、Storage Terminal、Crafting Terminal、左側 view buttons、>64 數量顯示,且 log 無 `advanced_container_set_data` / `ERROR` / `FATAL` / `Caused by`。
