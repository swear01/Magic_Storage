# Plan

- 2026-07-17 GitHub #2 Smithing Trim exact component-preserving slice：新增第十個exact built-in adapter，直接審計並委派1.21.1 `SmithingTrimRecipe`的template/base/addition predicates與`assemble`。Resolver只從目前Core/可選玩家背包的完整component stacks建立source-bound variants，以item registry ID + registry-aware canonical component NBT排序，65,536組合cap超限或component codec/trim registry context缺失時整體fail closed。同recipe ID的exact outputs在Craftable分開顯示；lookup/select/current/reload/execute全部按current holder與selected exact output重新綁定，不可退回base match，缺少exact identity且variant不唯一時也不猜第一個。Smithing Transform共用source-bound resolver但generic result lookup維持既有行為。Missing Smithing Table、stale/unsupported holder、full destination與execute-time output拒絕均no-op或完整rollback；special/dynamic/incomplete variants仍unsupported。Focused RED先得到static commit-path failure、321 GameTests中的canonical/Transform/menu failures，以及same-ID ambiguous request唯一失敗。Final GREEN：`compileJava`、`build`、EMI minimum/latest-compatible compile（本次兩者皆`1.1.24+1.21.1`）、SelfTest 224676/224676、GameTest 321/321、Python 190/190、Fusion/EMI-backed `runData` no-drift與`git diff --check`全部通過。
- 2026-07-17 Creative Storage lifecycle review：既有 unload/reload GameTest 改用 `GameTestSequence` 依序等待初始 Creative、遠端 chunk 卸載後 finite、觸發 reload、再等待 Creative，整段不手動呼叫 `core.rebuildNetwork`。首個 RED 重現 Core 保留 `StorageTypeCapacity[finiteTypeSlots=10, unlimited=true]` 的 stale cache；review RED 再用 `MAX_NETWORK_BLOCKS` 個沒有 loaded 外側連接的 boundary block 證明 pre-filter cap 會讓 reload 保持 finite。Production 改由 server-only FULL ticket transition 與延後的 server `LevelChunk` load event 維護 lifecycle；load queue 在下一 tick 以 non-loading `getChunkNow` one-shot 處理，無論是否就緒都移除且不逐 tick retry，後續 FULL ticket transition 再補排。有效 loaded boundary 先篩選再套 bound，同一 flush window 的 transient ticket pair 會取消，最後以 bounded multi-source BFS/full rebuild 更新 Core；保留 multi-Core conflict/完整拓樸語意且不 force-load、不做 periodic 整網掃描。Final gate 為 `compileJava`、SelfTest 224642/224642、GameTest 295/295、Python 185/185、EMI/Fusion/Patchouli-backed `runData` no-status-drift 與 `git diff --check`。
- 2026-07-17 Bus automation Phase 2a–2b：dist-neutral immutable `BusFilterPolicy`明確實作Allow-empty拒絕全部、Deny-empty允許全部、exact/item/tag/mod匹配、duplicate/unavailable可見但永不匹配，以及Allow依rule slot優先、Deny依全域item ID優先的決定性候選順序；同item variants再用registry-aware DataComponent codec與canonical NBT排序，不依hash/input順序。Policy與automation gate已接到既有directional active Import、passive Import與active Export；runtime config load清cooldown使新policy立即生效，Export drop保存owner-stripped non-exact rules。Menu packets、ownership enforcement、capability invalidation與directionless仍未實作。
- 2026-07-17 Bus automation Phase 1：Import/Export Bus 現在持久化schema 1 `busConfig`，包含mode、六面mask、unsided、automation、Allow/Deny、最多九條exact/item/tag/mod規則、owner與non-negative revision。舊Import/Export會精確遷移現有方向/能力/單一exact filter語意；unknown/malformed schema原樣保留並停用，缺席registry項目的rule也不會被靜默刪除。玩家放置會記錄owner；Export drop保留filter/config但移除owner與runtime Core cache。另新增完整`BusActor`/operation identity value objects，但Phase 1刻意仍以既有`Actor.bus(...)`執行搬運，directionless/filter/menu/security/re-entry behavior要等各自RED後才啟用。
- 2026-07-17 out-of-chunk Core storage repository：overworld `magic_storage_core_storages.dat`是inventory、energy、machines、descriptor state與raw unresolved NBT的唯一永久owner；Core chunk只寫`storageId`/`storageSchema`。inventory以每段最多63種、任意多段保存，63不影響785+ gameplay容量。runtime lease以storage UUID + dimension/position/token拒絕duplicate attachment；missing/corrupt/packed/duplicate全部fail closed。含資料Core的recovery UUID只指向同一record，drop與mapping都不複製payload，空Core移除才刪record；舊inline/snapshot/bottle/slot/axe格式不遷移。Prism lab reset也改成plain fresh Core，不再注入舊BE payload。
- 2026-07-17 built-in recipe-adapter authority：當時九個 exact built-in adapters 除 stable-ID classification/candidate indexing 外，也完成 ordered inputs/multiplicity、station descriptor ID、process/Fuel/tool cost、checked output/remainders、presentation semantics 與 simulation/commit validation。`CraftingTerminalMenu` 每次 select/preview/Max/execute 都按 recipe ID 重取目前 holder 並重新 classify，只保留通用 reservation、delivery、capacity 與 atomic mutation/rollback engine；`CraftingStationTable.java`、`RecipeEnergyTable.java` 已移除，unknown/custom family 無 generic fallback。EMI 只接受與目前 client `RecipeManager` holder 同一 identity 且通過 adapter contract 的 backing recipe。GitHub #2其後將現行family總數擴為十個；第三方family、公開addon API、多步驟planner與EMI tree仍未實作。
- Historical 2026-07-15 server-owned descriptor/Core recovery revision（已由2026-07-17 repository取代）：新增公開 NeoForge `magic_storage:machine_descriptor` registry、PROCESS/INSTANT/CONSUMABLE factories、server-frozen menu snapshot與clientbound finite/∞ state packet。Core/menu 使用固定 256 descriptor bank，machine與consumable NBT依 stable ResourceLocation 保存；該版曾把parseable incompatible stack搬回一般Core庫存；現行repository一律保留raw unresolved entry等待後續load。ordered snapshot只建一次並由Core tick/menu opening重用。該版曾建立完整SavedData recovery snapshot；現行token只指向建立時即存在的repository record，`/magic_storage recover_core`重發同一未claim capability。Remote開啟標題改為自己的「Remote Terminal／遠端終端機」。詳細addon契約見 `docs/machine-descriptor-api.md`。
- 2026-07-15 Fuel workspace density/extensibility：`11 / 785 types`改為player inventory正右方、上下對齊的獨立info panel，Instant Stations不再保留status格。Consumables/Timed Stations/Instant Stations從Crafting Terminal標題下方平均填滿到Inventory label band；每個descriptor grid依panel寬高計算columns × rows，超出容量時保留獨立頁碼與滾輪翻頁。SelfTest以每類64 descriptors覆蓋大量mod整合所需的layout/paging geometry；實際registration/sync由上方server-owned descriptor API完成。
- 2026-07-15 recipe/progression replacement：T1 移除紫水晶門檻，starter palette 改為 black concrete powder/obsidian/copper/redstone；T2–T6 以 basalt、obsidian、diamond/slime、prismarine/honeycomb/ender、netherite/amethyst 建立連續升級。Patchouli 新增 en_us/zh_tw Recipe Catalog，集中且不重複地顯示全部 13 個配方。recipe ledger 保持 1–4/5–8/9–12 三列規則，寬度不足 192px 時自動降低欄數，避免小視窗把材料格壓得過窄。
- 2026-07-15 guide/tool/terminal follow-up：指南改為 Magic Storage 自有 `guide_book` item 並新增 Book + Black Dye + Copper Ingot 配方，Recipe Catalog 擴為 14 個配方。Wrench 移除紫水晶，改為 Brush + Tripwire Hook；Storage/Crafting/Remote Terminal 分別對應起步存取、早中期合成與後期移動能力。材料充足數字由低對比亮綠改為淺色 vanilla panel 上可讀的深綠 `#176B2C`。

> 完整里程碑與設計見 `PLAN.md`(§八 開發里程碑 M1–M12)。狀態以程式碼為準(已有 91 Java 檔 + SelfTest/GameTest)。

M1–M11 大致到位。Shared-terminal/EMI recipe/Axe Energy revision 的 Task 1–10 production、TDD、active-doc audit 與 automated gates 已形成；0.1.17、0.1.18與0.1.19 的 fullscreen verdict 都是已取代的失敗歷史，不可拿來當目前 GUI 結論。現行 0.1.22 Prism dev 使用 macOS-only borderless Minecraft F11：不 attach GLFW monitor、不切換 desktop display mode，runner 在 READY 前後比對桌面 mode，使用者已確認目前全螢幕結果。正常關閉仍有硬性 gate：F11 → 正常有標題列的 windowed 視窗 → Command-Q；`shutdown.json` 是最後實際關閉結果。完整契約見 `docs/macos-fullscreen-guide.md`。

## Current State

- **Bus automation Phase 1 state foundation**：`BusConfiguration`/`BusFilterRule`已成為兩種Bus的server-owned versioned state，提供精確legacy defaults、raw unsupported preservation、owner capture與revision overflow防護；`BusActor`已能攜帶owner/legacy marker、dimension、bus position、network UUID、direction與operation ID。Phase 1本身只讓unsupported config fail closed；後續Phase 2b已把filter/automation接到既有directional搬運，但mode/side/unsided/owner與capability surface仍未完整接線。

- **Bus directional filter wiring**：`BusFilterPolicy`把最多九個raw rules編成保留slot/state的immutable snapshot，統一Allow/Deny、四種rule、duplicate/unavailable與deterministic candidate ordering；unsupported schema不會因Deny-empty而意外放行。現已server-side接到source-slot-order active Import、matching passive Import與rule-order active Export，且automation disabled三路徑都fail closed。這仍不是玩家可操作的完整Bus設定：packets、ownership、mode/side/unsided與capability invalidation尚未完成。

- **macOS F11 / close hardening**：`MacOsWindowMixin` 將 macOS Minecraft F11 改為 borderless Cocoa window，隱藏 Dock/menu bar 而不 attach GLFW monitor；`prepare_prism_gui_world.py` 移除 `fullscreenResolution`、記錄 desktop mode，`run_prism_gui_session.py` 在 READY 後精確比對點數、像素、refresh 與 depth。0.1.22 已部署，使用者已確認目前全螢幕視覺結果。關閉不以 READY 或 watchdog 取代：必須 F11 回到有標題列的 windowed 視窗後才 Command-Q；watchdog 僅記錄 `graceful` 或 `forced_after_glfw_shutdown_stall`。

- **Fuel/recipe vanilla-like presentation correction**：category-driven Fuel、compact target bar、full-height left-label/adaptive multi-row descriptor panels、inventory右側type-capacity info panel、Inventory label band、compact recipe card/最多四欄且top-aligned的三列ledger、vanilla light container panels與widgets與雙語 Patchouli 仍是現行內容。原本的 0.1.19 fullscreen 否決已由後續版本取代；目前 visual scenario 仍必須依每次變更重跑 current-run checklist，不能把歷史 READY 當成新版本驗收。

- **Shared terminal platform revision**：Task 1 已完成 exact display metadata、Craftable current-Core amount 與 Name/Quantity/Mod/ID comparator。Task 2 新增 capability-based `TerminalProfile`、唯一 `TerminalLayout.forProfile(...)` 與 shared `StorageTerminalScreen` shell；Storage 是 reduced profile，Crafting 不再 hide/recreate common controls。Rail 統一 18×18 hitbox/16×16 semantic-item 或 atlas icon，所有 cycle controls 使用 left-next/right-previous/wheel-down-next/wheel-up-previous，amount overlay 使用一個 screen-wide fixed scale。Task 3 再新增 server-built immutable `RecipePresentation`：exact ID/kind、positioned inputs、exact output components/count、station 與 typed resource rows，以 22 hidden slots同步；client workspace 改成 diagram→variable ledger→footer，完全不讀 client RecipeManager/Core。Task 4 讓 dev runtime 載入 full EMI；原本optional release policy已在2026-07-15改為client-only required`[1.1.24,2)`，base screen仍只依賴Magic Storage renderer interface，guarded adapter用exact ID/backing holder與公開widgets，unsupported internal recipe才native，ledger/footer維持本mod權威。Task 5 新增獨立 server-synced Player/Storage output destination，Player 是 inventory-first/Core-overflow，Storage 的 primary/remainder 全進 Core，整批容量不足則 mutation 前 no-op，EMI Cursor/Inventory 仍依自己的 request destination。Task 6 將九個 Fuel descriptors 分成五個 stackable process、三個 max-one instant、一道 consumable axe input；Core record以stable descriptor ID持久化installable stacks與finite/explicit-infinite Axe Energy，accepted axe原子消耗；現行格式不讀舊slot 8或legacy overstack payload。Synthetic axe preview/Max/commit 全改用 exact reserve，typed resource另同步 explicit infinity bit，blocked output rollback不扣值，per-key long-count mutation可一次處理 `Long.MAX_VALUE`；該階段 parity 曾為149 slots/100 data slots；目前descriptor API改為固定256格bank，因此現行總slots為396；Creative種類容量另加explicit-unlimited bit，現行data slots為101。Task 6 初始 RED 為 66 個 missing-API compile errors、Fuel UI static failure與兩個 GUI-lab/checklist failures，review RED另涵蓋 migration recovery/infinity/bulk commit；GREEN 為 compileJava、build、SelfTest 222949/222949、GameTest 251/251、full Python 99/99、`runData` no-new-drift、`runClient --dry-run`。Task 7 保留 shared cycle selector並加入 immutable `PopupList` geometry、代表物/名稱/selected rows、bounded scroll、outside/Escape/focus cleanup、dynamic EMI exclusion與 click-through/tooltip suppression；station/reserve tooltip只認實際 slot/icon。Task 7 GREEN 為 compileJava、SelfTest 222959/222959、GameTest 251/251、full Python 102/102、`runData` no-new-drift、`runClient --dry-run`。Task 8 用 fixed-seed Replicate shared chassis/palette建立16個model-referenced 16×16 RGBA assets、T1–T6 bars、Import/Export方向與fixed-canvas control atlas，移除4個runtime orphans；focused Python 2/2、full Python 103/103與resource build已綠。Task 9 consistency RED 抓到現行 GUI log 指令鎖死歷史 SelfTest/version；GREEN 改用 zero-failure 正則，並完成 0.1.16 版本/完整 gates/transactional Prism 部署。Task 10當時曾提供Bottle Energy→Glass Bottle migration/overflow escrow；2026-07-17 repository改為no-migration並只保留4-word wire parity；Import Bus 新增 all-side insert-only capability；Fuel popup 改 foreground layer，recipe 加 neutral no-recipe/station badge、Player/Core output icon，cycle tooltip 精簡，runtime en/zh keys 完全對齊；texture revision 以 71422–71424 seeds、x symmetry、fill bands與 cyan-in/orange-out bus grammar提高差異。Task 10 automated gates 已全綠並部署為 0.1.17；現在只剩 current-run READY 與使用者 fullscreen verdict，不可沿用舊 0.1.16 READY。

## Next Up

- **Bus automation Phase 2–4**：filter policy與既有directional transfers已接線；下一個RED slice加入server configuration/menu packets、ownership checks與capability invalidation。其後依序加入directionless capability matrix、structured actor guard、adversarial/modded handler與drop/move/reload完整收斂。不可把尚未接線的mode/side/unsided/owner欄位誤寫成玩家可用功能。
- **更多 context-dependent recipes**:exact `SmithingTransformRecipe`、exact `SmithingTrimRecipe`與deterministic axe strip/scrape/wax-off已完成；special/dynamic/incomplete、缺少trim registry mapping、event/player/world-state-dependent transformations、loom/cartography/grindstone/anvil/enchanting仍fail closed。只有能server-authoritatively建模完整input/output/components/side effects才可逐類加入。
- **Mod recipe + multi-step foundation**：現行十個 built-in families 已由完整 adapter match contract 單獨擁有 input/output/component/remainder/station/energy/tool/presentation/simulate/commit policy；非`minecraft` namespace但使用同一 exact vanilla class 可工作。custom `RecipeType`/class仍必須逐family提供同等完整 contract，禁止用`Recipe#getIngredients()`當通用fallback。2026-07-20 source audit選定Mekanism Enriching/Crushing作第一個正式API bounded adapter候選、AE2 Charger作第二候選；AE2 wrench與Sophisticated Storage item-handler另屬現行跨mod行為矩陣。任何外部mod只在有present-mod assertion時進獨立pin的compat job，不做只會boot的主CI膨脹。其後才做 bounded/cycle-safe、exact-component、energy/remainder/capacity-aware server graph。EMI公開API只用於recipe discovery/view；`BoM`/`MaterialTree`是internal，禁止直接依賴。詳見`docs/superpowers/plans/2026-07-14-modded-recipes-multistep-crafting.md`。
- **Brewing integration**:`BREW_ENERGY`/`blaze_fuel` 已可儲能但尚無 production recipe consumer；Glass Bottle/Potion 是一般 storage item，`bottle_fuel`不是玩家可選pool，現行repository格式也不讀或遷移該舊NBT。
- **P3 增量 grid delta(延後)**:A3 foundation 已完成;目前分頁式 grid(≤81 格,vanilla slot sync 已是增量)價值偏低,除非改成整列表 client grid 才值得做。
- **剩餘一般 deprecation(低優先)**:`ClientSetup` 的 for-removal event-bus warning 已消除；`-Xlint:deprecation` 仍列出 production `Level.hasChunkAt`、item registry holder 查詢，以及 GameTest `RegistryFriendlyByteBuf` 舊建構子。現行 NeoForge 21.1.228 可用且 tests 全綠，後續做純 API migration 時要保持 unloaded-chunk fail-closed 行為。

## Latest Verification

- 2026-07-20 EMI full-runtime source split：focused RED先以9個failures鎖住Modrinth exact runtime resolver、TerraformersMC API-only Gradle contract、兩版本staging CLI與CI/release/client-smoke wiring；第一次真實`stageEmiRuntime`再抓到Gradle `rename` lazy closure誤把`emi_version`當file property，補focused RED後改為configuration-time capture。Final `scripts/resolve_emi_runtime.py 1.1.24+1.21.1`解析`5sIPA1To`；Modrinth staging jar為1,111,538 bytes、SHA-1 `3fd494b37027dfbc7f78414d7cbf251c9eb6738a`且ZIP完整，`dependencyInsight`只列`maven.modrinth:emi:5sIPA1To`。`./gradlew build`、minimum/latest API compile（皆`1.1.24+1.21.1`）、Python 194/194、SelfTest 224676/224676、GameTest 326/326與Fusion/Modrinth-EMI/Patchouli-backed `runData`（0 files written、no drift）全綠。這次只改build/CI/docs，沒有GUI runtime畫面變更，不需要新增Prism visual verdict；AE2/Sophisticated Storage/Mekanism目前只有official-source target audit，尚未執行present-mod行為矩陣，不可先宣稱相容性已驗證。
- 2026-07-17 parallel wave 1 integration gate：GitHub #1 recipe-adapter authority、#6 Bus Phase 1–2b、#8 Creative Storage Unit/chunk lifecycle與#2 exact Smithing Trim已在同一integration tree交叉驗證。`./gradlew build`成功；`./gradlew runGameTestServer`為SelfTest 224676/224676、GameTest 326/326（包含Smithing branch的321項與parallel Creative lifecycle新增5項）；Python 190/190；EMI minimum/latest-compatible compile皆以`1.1.24+1.21.1`成功；Fusion/EMI/Patchouli-backed `./gradlew runData`寫入0檔且無resource drift；`git diff --check`通過。本wave未啟動Prism；Creative Unit既有GUI/texture變更仍需要current fullscreen manual verdict。
- 2026-07-17 Bus directional filter wiring：五個GameTests先在policy尚未接線時精確RED，鎖定active Import來源slot順序、passive Import simulate/execute與caller stack、active Export rule優先、automation disabled及non-exact Export drop。首次GREEN暴露runtime config套用後舊cooldown延遲兩條active路徑；`loadAdditional`清cooldown後final為`./gradlew build`、`./gradlew runGameTestServer`（SelfTest 224669/224669、GameTest 317/317）、Python 189/189、EMI minimum/latest compile、Fusion/EMI/Patchouli-backed `./gradlew runData`（0 files written、no resource drift）與`git diff --check`。
- 2026-07-17 Bus filter-policy foundation：六個新GameTests先以missing `BusFilterPolicy`產生22個compile errors；GREEN鎖定Allow/Deny empty、unsupported fail-closed、exact/item/tag/mod、duplicate/unavailable states、Allow rule-first與Deny global deterministic ordering，並用相反輸入順序證明同item component variants不依collection iteration。Final為`./gradlew build`、`./gradlew runGameTestServer`（SelfTest 224663/224663、GameTest 303/303）、Python 187/187、Fusion/EMI/Patchouli-backed `./gradlew runData`（0 files written、no status drift）與`git diff --check`。
- 2026-07-17 Bus automation Phase 1：先以8個新GameTests產生missing state/actor APIs的compile RED，再完成legacy defaults、component-exact Export filter migration、完整schema round-trip、unknown/malformed raw fail-closed、placement owner、structured actor與revision overflow。Review時發現新test class未加NeoForge holder annotation，補上後實際suite由289增為297，並新增全Java tree的`@GameTestHolder` static guard；Export drop regression另先RED證明新預設`busConfig`會遮蔽legacy component-rich filter，現改為drop同時保存無owner config並保留相容filter。Final為`./gradlew runGameTestServer`（SelfTest 224663/224663、GameTest 297/297）與Python 187/187；完整build/runData留給整合branch總gate。
- 2026-07-17 out-of-chunk Core repository final gate：TDD 依序鎖定固定大小 Core chunk NBT、0/1/63/64/785/786 inventory segment round-trip、same-record recovery、duplicate attachment、duplicate on-disk copies與malformed primitive/list/root/recovery NBT raw preservation、unsupported repository placement與失敗 claim 不殘留 temporary record。Final automated checks 為 `./gradlew build`、`./gradlew runGameTestServer`（SelfTest 224642/224642、GameTest 272/272）、`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover scripts`（181/181）、Fusion/EMI/Patchouli-backed `./gradlew runData`（no git-status drift）與 `git diff --check`。本次未改 GUI geometry/visual assets，因此不需新增 Prism visual verdict；舊 inline/snapshot/bottle/slot/axe payload 依已核准的 early-development policy 不遷移。
- 2026-07-17 built-in recipe-adapter authority：九個 table-driven family GameTests 先固定 exact non-`minecraft` holder 的 selection/preview/execute、components/output count/remainder、station、concrete cooking energy與finite/∞ axe delta；production把family policy完整移入 adapters，menu只保留通用transaction engine。Smithing weak-cache review另先以focused static RED證明value反向持有recipe，再改成只cache immutable item representatives。Final為`./gradlew build`、`./gradlew runGameTestServer`（SelfTest 224669/224669、GameTest 289/289）、Python 182/182與`git diff --check`；無GUI/runtime resource變更，未啟動Prism。
- 2026-07-17 recipe-adapter foundation：首個 SelfTest RED 以 missing `BuiltInRecipeAdapters` compile errors 固定 stable-ID exact-family selection；後續 focused RED 依序固定 priority/ID order、duplicate rejection、keyed lookup、九個 exact families、unknown/custom rejection、exhaustive/non-exhaustive candidates與same-ID reload holder boundary。Catalog GameTest 再以唯一 failure `A non-exhaustive displayed-item list omitted a matching recipe` 證明舊 index 會誤刪有效 custom ingredient。GREEN 新增 classification-only internal registry/candidate contract並只遷移 `CraftableRecipeCatalog`；menu execution/presentation/energy仍未搬動。Final為`compileJava`、`runGameTestServer`（SelfTest 224663/224663、GameTest 280/280）、Python 180/180、Fusion/EMI/Patchouli-backed `runData`（0 files written、no drift）與`git diff --check`全綠；沒有 GUI/runtime resource變更，因此未啟動 Prism。
- 2026-07-15 descriptor/Core recovery revision automated gate：TDD review先後以5個RED鎖定registry snapshot重建、descriptor item缺席時raw NBT遺失、一般Core庫存的缺席addon item遺失、失敗放置提前claim、同tick recovery重發順序；目前`runGameTestServer`為SelfTest 224642/224642、GameTest 279/279，且缺席item先查registry再parse，不再為可保留狀態輸出invalid-item ERROR。Final automated checks另含Python 180/180、`build`、Fusion/EMI/Patchouli-backed `runData`（0 generated files、no drift）、124 JSON parse、84-key en_us/zh_tw parity、`git diff --check`與`runClient --dry-run`全綠。這尚不是新jar的GUI verdict；舊0.1.29 Prism client仍在執行，必須先由使用者完成F11→windowed→Command-Q，才能部署並重跑`crafting-fuel-page`。
- 2026-07-15 Fuel full-height/multi-row correction：focused RED鎖定64 descriptors/category、61 targets與inventory-side status；GREEN後Python 173/173、`build`、SelfTest 224642/224642、GameTest 269/269、Fusion/EMI/Patchouli-backed `runData` no-status-drift與`git diff --check`全綠。部署器bump並部署唯一0.1.29，build/deployed SHA-256皆為`7bd6ac6d9e128892289bddb40a4e4134f3a209ae14e7310fb10298d3b26ccf42`。`build/gui-runs/20260715-143727-crafting-fuel-page/`已載入0.1.29、SelfTest 224642、Patchouli 46 JSON並到`MS_GUI_TEST_READY`；只有精確白名單的offline profile-properties 401。使用者其後明確回報該0.1.29 Fuel GUI通過；但client仍在執行且尚未完成F11→windowed→Command-Q關閉證據。descriptor/Remote title變更後仍必須以新jar重跑current-run visual gate。
- 2026-07-15 macOS borderless F11 resolution/close hardening：部署 0.1.22 至 Prism dev 後，`terminal-left-rail` live session 在 READY 前後的 macOS desktop mode 一致（1470×956 points、2940×1912 pixels、60 Hz）；Java 主視窗為 0,0、1470×956 的 onscreen borderless window，未變更 macOS 解析度。focused Python fullscreen/session tests 42/42、`compileJava`、`build` 均已成功；使用者確認目前全螢幕結果。關閉的正式 verdict 仍只看每個 run 的 F11 → windowed → Command-Q 與 `shutdown.json`，不得用 READY 或 watchdog forced cleanup 宣稱 graceful close。
- Historical 2026-07-14 post-0.1.19 dark-workspace replacement：new static test先在缺少shared terminal surfaces時RED；dedicated SelfTest再以8301 failures鎖定Fuel left-label/Inventory band與recipe compact/centering幾何。GREEN使用block-family palette、compact target bar、dim empty-station representatives、compact type cell、108px body/36px ledger caps與dark navigation/amount strip。Final為`compileJava`、`build`、Python 155/155、`runGameTestServer`（SelfTest 210828/210828、GameTest 269/269）、Fusion-backed `runData` no-new-drift與`git diff --check`。部署器bump至0.1.20，build/deployed SHA-256皆為`3d54527d8d04e930334d1ccddfb545b11f91183ae09074471a978f2683beb524`且Prism只有唯一Magic Storage jar；第一次`crafting-fuel-page` launch因primary monitor不可用而fail closed並清理，尚無current READY或fullscreen verdict。
- Historical 2026-07-14 macOS shutdown baseline：direct Command-Q in monitor-backed F11 could leave a 0×0 Java window and stall at `glfwSwapBuffers`; it motivated exact-process cleanup, `shutdown.json`, and the F11→windowed→Command-Q close gate. It is superseded for fullscreen entry by the 0.1.22 borderless implementation above; watchdog remains only the test-session final safeguard.
- 2026-07-14 0.1.19 screenshot correction：Fuel/recipe RED先由4個focused Python failures與18個missing geometry compile errors證明舊版仍是橫向icon/amount、Inventory-overlapping type capacity與空ledger第二層框。GREEN使用28px垂直cell、下方置中/縮放amount、Instant Stations右下固定status reservation與diagram+ledger unified empty surface；descriptor overflow會在reservation之外分頁。Final為Python 150/150、`build`、`runGameTestServer`（SelfTest 213726/213726、GameTest 269/269）、Fusion-backed `runData`且resource hash無new drift。部署器自動bump至0.1.19，build/deployed SHA-256同為`808fd6e8b36da98f2940cedf9c89aed3277fd64aee2186b3bbe444840b934082`；Prism只含唯一0.1.19與Fusion。`build/gui-runs/20260714-224250-crafting-fuel-page/`已以offline `MagicStorageBot`、wrapper/error-console disabled、automatic Minecraft F11到`MS_GUI_TEST_READY`；只有精確白名單的profile-properties 401。後續使用者fullscreen verdict已失敗，這個READY不可再作current visual baseline。
- Historical 2026-07-14 automatic fullscreen runner baseline：`fullscreen:true` and stale-resolution removal were introduced before the macOS borderless mixin. The former monitor-backed interpretation of “monitor current mode” is obsolete on macOS; the current contract is `minecraft_macos_borderless_fullscreen`, `minecraft_f11_borderless`, no monitor attach, and post-READY desktop-mode equality verification.
- 2026-07-14 connected-progression Task 7 automated/deployment/handoff gate：Fusion run-classpath static RED與真實`runData` RED先證明`AdditionalRuntimeClasspath`只把mod jar當legacy library，無法滿足NeoForge mod discovery；GREEN改成只供client/data runs使用的隔離`fusionRuntime` source set，GameTest server classpath明確不含Fusion。另一個static RED抓到publishing repository仍用Gradle 10將移除的space-assignment，改成`url = uri(...)`後`--warning-mode all`不再出現該build-script warning。Final為Python 146/146、compileJava/build、SelfTest 129163/129163、GameTest 269/269、runData載入Fusion 1.2.12且datagen hash set無drift、runClient dry-run、13組connected PNG/metadata、雙語各22個Patchouli JSON、無embedded Fusion/nested jar與`git diff --check`全綠。部署器自動bump至0.1.18；build/deployed SHA-256同為`d4219e441668b0262cbb6247ec0f796d4cf3c5fa9738b1eb47173783c15764bc`，Prism mods只有唯一0.1.18與Fusion SHA-512 `50604fa…365c`。第一次launch因primary monitor不可用而fail-closed並清理；activity wake後同scenario在`build/gui-runs/20260714-211556-crafting-fuel-page/`到READY，log載入Magic Storage 0.1.18、Fusion 1.2.12、SelfTest 129163/129163與Patchouli總計44 JSON（每語系22）。只有精確白名單的offline profile-properties 401；目前client刻意保留給使用者進fullscreen，visual verdict仍待確認。
- 2026-07-14 shared-terminal Task 10：先以 SelfTest/GameTest/Python contracts 證明 live Bottle Energy、缺少 migration/overflow recovery、缺少 all-side passive Import、popup layer/no-recipe/station/output icon/i18n/texture contracts；production 退役 live pool、保留 lossless Glass Bottle escrow migration 與 100-data-slot parity，Import Bus 新增 all-side insert-only capability但保留 front active pull，並完成 concise GUI、exact en/zh parity及71422–71424對稱差異化材質。Review RED另證明 passive no-Core probe會立即重跑BFS、飽和 bottle escrow 在 crafting transaction 內提前補回會破壞 rollback，現分別以 shared 10-tick negative cache與 outermost-batch refill修正；Auto tie-break coverage及繁中 Brew Energy語意也已補強。Final automated gate 為 `compileJava`、`build`、`runGameTestServer`（SelfTest 222956/222956、GameTest 262/262）、Python 110/110、`runData` no-new-drift、`runClient --dry-run`、78 JSON resources與`git diff --check`全綠。0.1.17 已原子部署且 build/deployed SHA-256 同為 `3da6203ba16e3ad0a5b090955e5214dbf4fb33415c8b0bb06620fadf26caa93e`；尚待 current-run READY 與 fullscreen verdict。該段 Bottle Energy/escrow migration 僅是歷史行為，已由 2026-07-17 no-migration repository revision 取代。
- 2026-07-14 shared-terminal Task 9：active-doc audit 確認 installed-axe、variable ledger、shared terminal、Name/Quantity/Mod/ID 與 Player/Storage output 的現行契約已一致；新 static RED 只抓到 manual GUI log 指令鎖死 `SelfTest 40608`/0.1.15，改成驗證任意非負 totals 且 `0 failed` 後 GREEN。0.1.16 final gate 為 `compileJava`、`build`、`runGameTestServer`（SelfTest 222959 + GameTest 251）、Python 104/104、`runData` no-new-drift、`runClient --dry-run`、78 JSON/model/texture/jar validation。Deploy script 已備份 0.1.15 並原子部署唯一 `magic_storage-0.1.16.jar`，build/deployed SHA-256 同為 `2ca800b1edbf643f278064d1d3392414fc2c48b529a1003b3e2ce7d6fbbd350a`。使用者 fullscreen verdict 尚待 current-run handoff。

- 2026-07-14 shared-terminal Task 8：兩個 focused static contracts 先以 orphan `import_bus.png`/`export_bus.png`、32×32 runtime、obsolete `grid.png`/`crafting_grid.png`與missing selection manifest形成 RED。Replicate seed 71421 + selected chassis 04建立9組role candidates，0.38 block/0.68 item img2img後以14色palette、共用chassis mask與semantic cleanup選定16個runtime members；三張contact sheets先檢查後才替換。Tests鎖 exact model/runtime set、16×16 RGBA、portable metadata/hash、shared chassis pixels、T1–T6 bars、Core/terminal/import/export/remote accents、Remote透明角、contiguous 16px white atlas cells與無sidecars。Focused Python 2/2、full Python 103/103、`build`與jar resource inventory已綠；完整GameTest/datagen與fullscreen textures留給Task 9。

- 2026-07-14 shared-terminal Task 7：SelfTest 先以 missing `fuelTargetListButton`/`PopupList`/exact hitbox APIs compile errors形成 RED，三個 static contracts 再固定 popup/input/EMI/hover failures；overlay review另以 click-through/covered-slot tooltip failure與 Prism checklist failure形成 RED。Production 保留 shared cycle方向，新增 descriptor-driven bounded popup、selected state、outside/Escape/page-close focus cleanup、dynamic EMI exclusion與底層 slot input/tooltip suppression；flow padding不再觸發 station/reserve tooltip。`compileJava`、`build`、`runGameTestServer`（SelfTest 222959 + GameTest 251）、full Python 102/102、`runData` no-new-drift、`runClient --dry-run` 已綠；fullscreen popup verdict仍待整批最終 gate。

- 2026-07-15 EMI dependency range：先以9個focused failures鎖住release client metadata、range/baseline一致性、numeric latest resolver與CI/release/client-smoke gates；後續實際下載RED再抓到direct curl 92 partial jar與`run/mods`/plain`runtimeOnly`污染dedicated GameTest，改由isolated`emiRuntime` staging task寫`build/client-smoke-mods`，full EMI dev dependency移到`fusionRuntimeRuntimeOnly`。Release現在required on CLIENT、範圍`[1.1.24,2)`，dedicated server不要求。`1.1.24+1.21.1`只作minimum compile baseline；CI與release仍以TerraformersMC dedicated API metadata/artifact驗minimum/latest，2026-07-20起full client/data jar則由Modrinth API exact mapping + Modrinth Maven供應，不再從TerraformersMC抓大檔。任何解析、映射、下載或編譯失敗皆fail closed。EMI compat source另由static test禁止`bom`/`screen`/`runtime` internal packages。
- 2026-07-14 shared-terminal Task 4：5 個 focused static failures 先鎖住 full EMI dev runtime、無 EMI base boundary、public widget-only、exact selection、bounded input/tooltip 與 no broad catch。Production 新增 guarded bootstrap、native/public-widget renderers，標準配方要求 EMI exact ID + backing holder 一致，internal axe 明確 native；dev 使用 full EMI `runtimeOnly`。當時release metadata不required/bundle；optional部分已被2026-07-15 range policy取代，仍不bundle。`compileJava`、`build`、`runGameTestServer`（SelfTest 222946 + GameTest 231）、focused Python 48/48、full Python 95/95、`runData` no-new-drift、`runClient --dry-run` 已綠；本機官方 Maven GET 截斷採使用者核准的 same-URL range prefetch，只寫 Gradle cache。尚未做 fullscreen EMI widget verdict。

- 2026-07-14 shared-terminal Task 3：首批 5 個 GameTest 先產生 28 個 missing exact-presentation compile errors；layout SelfTest 再產生 31 個 missing geometry compile errors；focused Python 先固定 4 failures；提交前 3 個額外契約測試再精確重現 oversize shaped、same-type ID ordering、exact navigation reload 問題。Production 新增 exact immutable model、149/94 slot-data parity、real menu-wire reconstruction、positioned diagram、dynamic typed ledger、exact output count/tooltips，並 fail closed 於超過 3×3 的 shaped recipe。`compileJava`、`build`、`runGameTestServer`（SelfTest 222946 + GameTest 231）、focused Python 43/43、full Python 90/90、`runData` 與 datagen no-new-drift 已綠；尚未做 fullscreen GUI verdict。
- 2026-07-14 shared-terminal Task 2：SelfTest 先因 missing `TerminalProfile`/`TerminalCycleDirection`/`previous()`/fixed-scale API 產生 41 個 compile errors；subagent static suite 再抓到 Fuel target 仍用 vanilla `CycleButton`。Production 改為單一 profile layout entry、base-owned common shell/controls、18px/16px item-or-atlas icons、統一 click/wheel cycle、shared focus cleanup 與 fixed amount scale；server 增加 previous sort/search IDs 並有 menu integration assertions。`compileJava`、`runGameTestServer`（SelfTest 40999 + GameTest 223）與 focused Python 37/37、full Python 84/84 已綠；尚未做 fullscreen GUI verdict。
- 2026-07-13 true-void GUI lab replacement：先以 6 個 focused RED 鎖住 missing void rewrite/state stripping/lab manifest/readiness/checklist，再加 embedded `Data/Player` RED，完成 exact flat-air codec、spawn rewrite、source/player isolation、Y=80 active network + isolated gallery、預載 Core/stations、fixed player kit、3-tick reusable reset 和 `Installed Stations` handoff。Final `build`、SelfTest 40608、GameTest 222、Python 76/76、`runData`、datagen no-drift 全綠；`20260713-221433-crafting-fuel-page` current-run 有 SelfTest/Patchouli 17/READY，僅 offline profile-properties 401 白名單。Minecraft 已開給使用者，visual verdict 未完成。
- 2026-07-13 0.1.15 focused TDD：full/partial inventory、Core overflow/full rejection、direct/EMI/Max/long-count/largest-deliverable output 路徑；9-slot station/tool gating；component-exact/preserving Smithing Transform；vanilla axe strip/scrape/wax-off + raw durability + blocked-output atomicity；two-row/full-width Fuel geometry + occupied lower-right control panel；model-referenced 16×16 texture static contract均已 RED→GREEN。Final versioned automated gates為 build、SelfTest 40608 + GameTest 222、Python 72/72、runData、JSON parse 與 datagen no-drift；deployment/fullscreen handoff 由使用者延後。當時 contract：`docs/superpowers/plans/2026-07-13-stations-recipes-output-textures.md`；現行 replacement 見 2026-07-14 shared-terminal plan。
- 2026-07-13 0.1.14 Prism handoff：`PYTHONDONTWRITEBYTECODE=1 python3 scripts/run_prism_gui_session.py --scenario crafting-fuel-page` 成功，`build/gui-runs/20260713-141850-crafting-fuel-page/` 含 manifest/session/checklist/log excerpt；current-run log 證明 Magic Storage 0.1.14、SelfTest 40406/40406、`MS_GUI_TEST_READY` 與 offline player `MagicStorageBot`。已通過自動 READY gate並保留 Minecraft，fullscreen 視覺 checklist 仍由使用者執行與判定。
- 2026-07-13 Task 9 Fuel selector/layout：先以 missing selector/rail geometry 的 5 個 compile errors、3 個 static contracts、Prism checklist 確認 RED；client layout 完成後，再以 GameTest 證明 retired ID 17 確實改動 target。Production 使用 header vanilla `CycleButton`、Fuel rail 僅三頁籤、server 僅 Auto/exact IDs、page-aware proportional flow cells，且 selector wheel 優先於 reserve paging。`compileJava`、`build`、`runGameTestServer`（SelfTest 40406 + GameTest 210）、Python 70/70、`runData`、77 JSON parse 與 datagen no-new-drift 全綠；final 0.1.14 取代測試強化前的 0.1.13 intermediate，唯一 Prism jar 與 build jar SHA-256 同為 `97069ca985797ce237b421df2fc5c08c44cc2ca4dedf1957d0f756c2c92dae75`。只剩 fullscreen gate。
- 2026-07-13 0.1.12 fullscreen follow-up：代表物/compact amount 自動 gate 已完成，但實機否決 sparse descriptors 左聚集與 previous/next/current target 顯示語意。當時 Task 9 contract 改成 CycleButton current-value selector、compact Fuel rail、balanced flow，後續已由上方 0.1.14 verification 完成。
- 2026-07-13 Task 8 representative amount/type-capacity revision：先以 missing formatter/representative APIs 與 Python contracts 確認 RED；再新增完整八種代表物、compact K/M/G/T/P/E、16px normal/0.5-scale cell-local right alignment、Fuel type-capacity wire parity。額外 GameTest 以 `menu=10/core=35` 重現 open Fuel 頁 topology capacity stale，再修成 topology revision 變更即 refresh metadata。`build`、`runGameTestServer`（SelfTest 40340 + GameTest 209）、Python 70/70、`runData`、77 JSON parse 與 datagen no-new-drift 全綠；deploy 自動 bump 0.1.11→0.1.12，唯一 Prism jar 與 build jar SHA-256 同為 `f7b0e78e630c03f77331fa9aad4c4d3f04512d069b327c9bad6942a0c8844309`。只剩 fullscreen gate。
- 2026-07-12 Task 7 three-page/scalable Fuel revision：先確認 Python checklist/static 與 compileJava 因缺少新契約而 RED，再完成 Storage/Craftable/Fuel、94 data-slot parity、synthetic extraction/EMI source boundary、fresh uncapped Max、live exact-button state、grouped fixed-width target rail、full-width descriptor FlowGrid/paging、shadowless values 與 semantic search icons。額外 RED 證明 10,005 次合法 craft 不可被 9,999 wire preview 截斷，且 60 reserve descriptors 不可讓 rail 出界。`compileJava`、`build`、`runGameTestServer`（SelfTest 40321 + GameTest 208）、Python 69/69、`runData`、77 JSON parse 與 datagen drift checks 全綠；0.1.11 同版本最終 jar transactionally 取代舊部署，唯一 Prism jar 與 build jar SHA-256 同為 `09fd5ca4e2b2d41c1163daa255da58232be021f1a131f30e53d80748f37b6d1d`。只剩 fullscreen gate。
- 2026-07-12 Task 6 adaptive/resource revision：先加入會失敗的 ingredient/process/Fuel preview、full-`ItemKey`、Compact removal、95-slot signed-short parity、complete-geometry margins/centering/resource cells 與 fixed icon canvas tests，再完成最小 production 實作。`compileJava`、`build`、SelfTest 39823 + GameTest 205、Python 68/68、`runData`、77 JSON parse、diff/drift checks 全綠；deploy 自動 bump 0.1.9→0.1.10，Prism 唯一 jar 與 build jar SHA-256 同為 `2697c631ead5351af5bed7803656f1b1a80f3c0aed83e496e2a8087090afd59b`。只剩 fullscreen gate。
- 2026-07-12 Dynamic Fuel/Craftable/EMI/adaptive GUI 0.1.9 gate：`./gradlew compileJava`、`./gradlew build`、`./gradlew runGameTestServer`（SelfTest 6610 + GameTest 204）、`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover scripts`（66/66）與 `./gradlew runData` 全綠。Deploy script 自動 bump 0.1.8→0.1.9，Prism mods 只有 `magic_storage-0.1.9.jar`，與 build jar SHA-256 同為 `22f20cc3f38d8bee3160cf91be8c32d8d4faa9ed9582bc89a02cb1a6ce00d858`。新增 runtime oak-log Fuel、concrete cookingtime preview/execute、zero-storage recipe catalog/reload/search/sort、EMI exact ID/amount/destination/atomic capacity、320×240 到 600 高度 exhaustive geometry、original semantic slot delegates、vanilla-like palette、focus cleanup 與 EMI exclusion regressions；新 jar fullscreen gate 尚待使用者執行。
- 2026-07-12 Fuel/machine + deep correctness gate:`./gradlew build --console=plain --no-daemon`、`./gradlew runGameTestServer --console=plain --no-daemon`（SelfTest 117 + GameTest 180）、`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover scripts`（61/61）與 `./gradlew runData --console=plain --no-daemon`（resource snapshot 無 drift）全綠。新增/守護:ItemKey component snapshot、Locale.ROOT/compact identity ordering、display click/shift extraction whitelist、11/52 signed-short wire parity、absolute scroll packet、搜尋 state re-init、EMI player-source toggle、live player/topology preview、currently-loaded cached path、deferred-listener atomic craft commit、unknown/no-op packet 去重與 preview 一次 source snapshot；GUI world/Prism runner/deploy 的 staging、hash、rollback、runner-owned process cleanup 與 primary-monitor actionable failure 也有故障注入測試。部署器已自動升至 0.1.8，Prism mods 只留一個且與 build jar SHA-256 相同。
- 2026-07-11 second correctness pass:`./gradlew runGameTestServer --console=plain --no-daemon` 已驗證 SelfTest 104 + GameTest 141。新增/守護:lava bucket 歸還空桶、fuzzy matching / NBT duplicate / Core+player preview long 飽和、EMI 只傳 exact supported backing recipe id 且 server 重驗、Smithing fail closed、活塞 old/new topology、程式化放置 coalesced next-tick growth、增量/full-rebuild depth parity，以及 Import/Export bus 在 reentrant commit/rollback 改變時精確歸還或顯式掉出 remainder。
- 2026-07-11 full correctness audit:`./gradlew runGameTestServer --console=plain --no-daemon` 已驗證 SelfTest 104 + GameTest 131。新增/守護:Core drop/NBT identity persistence、long item/energy saturation、non-player removal rebuild、open terminal live refresh/scroll sync/disconnect、search packet/tag validation、legal shift extraction、conflicted/incomplete recipe rejection、overlapping ingredient joint reservation + reroute、crafting container remainder、terminal fuel popup/server validation/preview refresh、Remote exact-Core UUID/dimension pinning與 legacy binding fail-closed。
- 2026-07-12 Prism GUI manual handoff hardening:`scripts/setup_prism_computer_use_wrapper.py` 只保留作選用診斷；正常 `scripts/run_prism_gui_session.py` 強制 `WrapperCommand=` + `OverrideConsole=true` + `ShowConsole=false` + `ShowConsoleOnError=false`，產生 `build/gui-runs/<timestamp>-<scenario>/` artifacts、以 native Prism offline player `-o MagicStorageBot` 跳過 Microsoft/Xbox account refresh、用 offset+inode cursor 只讀本次 log、區分 `boot-smoke`(不需目視)與 visual scenarios。Visual scenario 到 `MS_GUI_TEST_READY` 後明確輸出 handoff message 並停止自動 GUI 操作，由使用者完成 fullscreen gate/checklist。offline UUID 仍可能把 profile-properties 401 寫進檔案，但不再自動彈 Prism console；runner 只白名單此已知 stack，其他 ERROR/Caused by 仍 fail。21:04 舊 wrapper run 已確認於 21:10 `Stopping!` 後退出，不可拿舊 schema artifact 當新流程證據。
- 2026-07-10 P0/P1/P2 RS2 parity + test expansion:`./gradlew compileJava`、`./gradlew runGameTestServer` 已驗證 SelfTest 104 + GameTest 102。新增/守護:Action/Actor storage ops、per-resource listener events、simulate no event、type-slot reuse、bus cached-core disconnect invalidation、import bus type-full no extraction、export respects target capacity、invalid tag filter no throw、incremental placement bridge fallback、multi-core bridge conflicted、terminal access/remote `stillValid`、display pickup stack amounts、craftable-only server filter、server-synced crafting recipe metadata/hidden slots parity、EMI no hidden metadata slots、real crafting/smelting/player-inventory craft paths、duplicate ingredients aggregate before craft；當時的初始 compact grid 後來已由 Task 6 完整移除。
- 2026-07-10 ClientSetup NeoForge deprecation cleanup:`@EventBusSubscriber(bus = EventBusSubscriber.Bus.MOD)` 已移除,client-only `RegisterMenuScreensEvent` 改由 `FMLEnvironment.dist == Dist.CLIENT` guard 後用 `modEventBus.addListener(ClientSetup::registerScreens)` 註冊;Python static regression 守護不回退。
- 2026-07-09 初版 GUI test rig（歷史，已由上方 true-void lab 取代）：當時以 `prepare_prism_gui_world.py` 重建固定 world、無 command block datapack、hotbar view 與 condition polling，並實測 `1`/`2` 在 fullscreen 下開 Storage/Crafting Terminal；不可再把這筆當成目前 world/layout 的驗證。
- 2026-07-09 CI/CD 本地驗證目標:`.github/workflows/ci.yml` 跑 build + GameTest + Python unittest + datagen drift check + jar/log/report artifacts;`.github/workflows/release.yml` 僅接受 tag `v<mod_version>`,重跑同一組 gates,產 release notes 並用 GitHub CLI 建 release。
- 2026-07-08 Prism dev 實機驗證（歷史流程，不可沿用 fullscreen 方法）:`magic_storage-0.1.3.jar` 已部署到 dev instance,mods 內僅此一個 Magic Storage jar。當時以 `open -a "Prism Launcher" --args -l dev -w "New World"` 直進世界並切 macOS native fullscreen；Computer Use 目視確認 Patchouli guide、Storage Terminal、Crafting Terminal、左側 view buttons、>64 數量顯示。現行 runner 已取代該 world/launch/fullscreen 流程，且明確禁止 macOS native fullscreen。
